---
phase: 08-kaavya-reader-and-storage-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/components/KaavyaLibrary.tsx
  - src/app/components/LibraryCard.tsx
  - src/app/components/KaavyaUploader.tsx
  - src/app/page.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - READ-01
  - READ-02
  - READ-06

must_haves:
  truths:
    - "User can browse their library of uploaded kaavyas"
    - "User can upload a PDF and have its text extracted and saved"
    - "User can paste Sanskrit text and save it as a kaavya"
    - "Library updates reactively when a kaavya is added or deleted"
  artifacts:
    - path: "src/app/components/KaavyaLibrary.tsx"
      provides: "Library grid view with empty state, loading, and populated states"
      contains: "useKaavyaLibrary"
    - path: "src/app/components/LibraryCard.tsx"
      provides: "Individual kaavya card with title, reading progress, delete"
      contains: "LibraryCard"
    - path: "src/app/components/KaavyaUploader.tsx"
      provides: "PDF upload dropzone + text paste textarea with title input"
      contains: "extractTextFromPdf"
    - path: "src/app/page.tsx"
      provides: "Top-level routing between Analyze, Library, and Reader views"
      contains: "KaavyaLibrary"
  key_links:
    - from: "src/app/components/KaavyaLibrary.tsx"
      to: "src/lib/kaavya/hooks/useKaavyaLibrary.ts"
      via: "useKaavyaLibrary hook for reactive listing"
      pattern: "useKaavyaLibrary"
    - from: "src/app/components/KaavyaUploader.tsx"
      to: "src/lib/kaavya/db/kaavyaStore.ts"
      via: "saveKaavya on form submit"
      pattern: "saveKaavya"
    - from: "src/app/components/KaavyaUploader.tsx"
      to: "src/lib/kaavya/utils/pdfExtractor.ts"
      via: "extractTextFromPdf on file upload"
      pattern: "extractTextFromPdf"
---

<objective>
Build the Kaavya Library and Uploader UI -- users can browse their saved kaavyas in a card grid, upload PDFs, paste text, and save documents to their local library.

Purpose: This is the entry point for the kaavya reading experience. Users need to see their library and add new texts before they can read them.
Output: KaavyaLibrary, LibraryCard, KaavyaUploader components, and updated page routing.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-kaavya-reader-and-storage-foundation/08-RESEARCH.md
@.planning/phases/08-kaavya-reader-and-storage-foundation/08-UI-SPEC.md
@.planning/phases/08-kaavya-reader-and-storage-foundation/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/kaavya/types.ts:
```typescript
export interface Kaavya {
  id?: number;
  title: string;
  author?: string;
  sourceType: 'pdf' | 'pasted';
  rawText: string;
  createdAt: Date;
  updatedAt: Date;
}

export interface ReadingState {
  id?: number;
  kaavyaId: number;
  currentPage: number;
  totalPages: number;
  lastReadAt: Date;
}
```

From src/lib/kaavya/db/kaavyaStore.ts:
```typescript
export async function saveKaavya(title: string, rawText: string, sourceType: 'pdf' | 'pasted', author?: string): Promise<number>;
export async function getKaavya(id: number): Promise<Kaavya | undefined>;
export async function getAllKaavyas(): Promise<Kaavya[]>;
export async function deleteKaavya(id: number): Promise<void>;
```

From src/lib/kaavya/hooks/useKaavyaLibrary.ts:
```typescript
export function useKaavyaLibrary(): { kaavyas: Kaavya[], isLoading: boolean };
```

From src/lib/kaavya/utils/pdfExtractor.ts:
```typescript
export async function extractTextFromPdf(file: File): Promise<string>;
```

From src/app/globals.css (theme tokens):
- parchment-50: #fdf8f0 (page bg)
- parchment-100: #f9edd8 (card bg)
- parchment-200: #f3dbb1 (borders)
- ink-800: #2a1f14 (text primary)
- ink-700: #3d2e1f (text secondary)
- accent-500: #b45309 (CTAs)
- accent-600: #92400e (hover)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LibraryCard and KaavyaLibrary components</name>
  <files>
    src/app/components/LibraryCard.tsx,
    src/app/components/KaavyaLibrary.tsx
  </files>
  <read_first>
    src/app/components/AnalysisView.tsx,
    src/app/components/ImageUpload.tsx,
    src/app/globals.css,
    src/lib/kaavya/types.ts,
    src/lib/kaavya/hooks/useKaavyaLibrary.ts,
    src/lib/kaavya/db/kaavyaStore.ts,
    .planning/phases/08-kaavya-reader-and-storage-foundation/08-UI-SPEC.md
  </read_first>
  <action>
1. Create `src/app/components/LibraryCard.tsx` -- "use client" component:
   - Props: `{ kaavya: Kaavya, readingState?: ReadingState, onOpen: (id: number) => void, onDelete: (id: number) => void }`
   - Card layout: parchment-100 bg, parchment-200 border (1px solid), rounded-lg (8px radius), p-4 (16px padding)
   - Display: title (text-lg font-semibold text-ink-800), author if present (text-sm text-ink-700)
   - Reading progress: "Page {n} of {total}" or "Not started" in text-sm text-ink-700
   - Last activity: relative time display ("Last read 2d ago" or "Added today") in text-xs text-ink-700
   - Hover state: shadow-md transition-shadow duration-200
   - Click handler: calls onOpen(kaavya.id!)
   - Delete button: small trash icon (FaTrash from react-icons/fa) in top-right corner, visible on hover, red-600 color
   - Delete confirmation: window.confirm("This will permanently remove {title} from your library. Reading progress and interpretation history will be lost.")
   - Helper function for relative time: compute "today", "yesterday", "Xd ago", "Xw ago" from Date

2. Create `src/app/components/KaavyaLibrary.tsx` -- "use client" component:
   - Props: `{ onOpenKaavya: (id: number) => void, onAddKaavya: () => void }`
   - Use `useKaavyaLibrary()` hook for reactive data
   - Use `useLiveQuery` from dexie-react-hooks to also fetch readingStates for each kaavya
   - Top bar: "Library" heading (text-2xl font-semibold) + "Add Kaavya" button (accent-500 bg, white text, rounded-lg, px-4 py-2)
   - Loading state: 2-column grid of 4 skeleton cards (parchment-100 bg with animate-pulse, rounded-lg, h-32)
   - Empty state: centered layout with "Your library is empty" heading (text-xl font-semibold text-ink-800) + "Upload a PDF or paste Sanskrit text to start reading. Your kaavyas are stored locally and persist across sessions." body (text-ink-700) + "Add Kaavya" CTA button (accent-500)
   - Populated state: grid grid-cols-1 sm:grid-cols-2 gap-4, render LibraryCard for each kaavya
   - Delete handler: calls deleteKaavya(id) from kaavyaStore, useLiveQuery auto-updates
  </action>
  <verify>
    <automated>grep -q "useKaavyaLibrary" src/app/components/KaavyaLibrary.tsx && grep -q "LibraryCard" src/app/components/LibraryCard.tsx && grep -q "onDelete" src/app/components/LibraryCard.tsx && grep -q "Your library is empty" src/app/components/KaavyaLibrary.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/components/LibraryCard.tsx contains `"use client"`, `export function LibraryCard`, `onDelete`, `onOpen`, `parchment-100`
    - src/app/components/KaavyaLibrary.tsx contains `"use client"`, `export function KaavyaLibrary`, `useKaavyaLibrary`, `Your library is empty`, `Add Kaavya`, `grid-cols-1 sm:grid-cols-2`
    - LibraryCard shows relative time, reading progress, and delete confirmation
    - KaavyaLibrary has loading/empty/populated states
  </acceptance_criteria>
  <done>Library view shows kaavya cards in a responsive grid with delete support, empty state, and loading skeletons.</done>
</task>

<task type="auto">
  <name>Task 2: Create KaavyaUploader and wire page routing for Library tab</name>
  <files>
    src/app/components/KaavyaUploader.tsx,
    src/app/page.tsx,
    src/app/layout.tsx
  </files>
  <read_first>
    src/app/page.tsx,
    src/app/layout.tsx,
    src/app/components/AnalysisView.tsx,
    src/app/components/ImageUpload.tsx,
    src/lib/kaavya/utils/pdfExtractor.ts,
    src/lib/kaavya/db/kaavyaStore.ts,
    .planning/phases/08-kaavya-reader-and-storage-foundation/08-UI-SPEC.md
  </read_first>
  <action>
1. Create `src/app/components/KaavyaUploader.tsx` -- "use client" component:
   - Props: `{ onSaved: (kaavyaId: number) => void, onCancel: () => void }`
   - State: title (string), rawText (string), sourceType ('pdf' | 'pasted'), isExtracting (boolean), error (string | null), extractedPreview (string)
   - Title input: text input with placeholder "Kaavya title (e.g., Meghadutam)", border parchment-200, focus:ring-accent-500, full width
   - Toggle tabs between "Upload PDF" and "Paste Text" -- two buttons styled as tabs (active = accent-500 text + bottom border, inactive = ink-700)
   - PDF Upload tab:
     - Drag-and-drop zone: dashed 2px parchment-200 border, parchment-100 bg, py-12 (48px vertical padding), rounded-lg
     - Center content: FaFilePdf icon (accent-500, text-3xl), "Drop a PDF here, or click to browse" text
     - Hidden file input accept=".pdf", triggered on zone click
     - Drag events: onDragOver/onDragEnter change border to solid accent-500, bg to accent-500/10; onDragLeave reverts
     - onDrop/onChange: call extractTextFromPdf(file), set isExtracting=true during extraction
     - Extracting state: show "Extracting text from PDF..." with inline spinner (animate-spin)
     - Error state: "Could not extract text from this PDF. The file may be scanned or image-based. Try pasting the text directly instead." in red-600
     - After extraction: show preview in read-only textarea (font-sanskrit, max-h-48 overflow-y-auto, parchment-100 bg)
   - Paste Text tab:
     - Textarea: min-h-[200px], font-sanskrit (Shobhika), placeholder "Paste Sanskrit text here...", parchment-100 bg, border parchment-200
   - Save button: "Save to Library" -- accent-500 bg, white text, rounded-lg, px-6 py-3, disabled (opacity-50) when title is empty or rawText is empty
   - Cancel button: text-ink-700, mr-4 next to Save button
   - On save: call saveKaavya(title, rawText, sourceType), then call onSaved(returnedId)

2. Update `src/app/page.tsx` to add Library tab:
   - Add top-level state: `view: 'analyze' | 'library' | 'uploader' | 'reader'` and `selectedKaavyaId: number | null`
   - Add tab navigation bar at top (before main content): three tabs -- "Analyze" (FaSearch icon), "Library" (FaBook icon), same style as existing AnalysisView internal tabs
   - Tab bar: flex, bg-parchment-100, rounded-xl, p-1, mb-6; each tab = flex-1, py-2, text-center, rounded-lg; active tab = bg-white shadow-sm text-accent-500, inactive = text-ink-700
   - When view === 'analyze': render existing AnalysisView
   - When view === 'library': render KaavyaLibrary with onOpenKaavya={(id) => { setSelectedKaavyaId(id); setView('reader'); }} and onAddKaavya={() => setView('uploader')}
   - When view === 'uploader': render KaavyaUploader with onSaved={(id) => { setSelectedKaavyaId(id); setView('reader'); }} and onCancel={() => setView('library')}
   - When view === 'reader': render placeholder div "Reader coming in Plan 03" (temporary) with a "Back to Library" button
   - Wrap everything in "use client" -- the page.tsx needs to become a client component for state management. Move metadata to layout.tsx if not already there.

3. Update `src/app/layout.tsx`:
   - Update page title to "Sanskrit Learning Platform" (from "Sanskrit Analyzer")
   - Update description to include "kaavya reading" mention
  </action>
  <verify>
    <automated>grep -q "extractTextFromPdf" src/app/components/KaavyaUploader.tsx && grep -q "saveKaavya" src/app/components/KaavyaUploader.tsx && grep -q "KaavyaLibrary" src/app/page.tsx && grep -q "library" src/app/page.tsx && grep -q "Save to Library" src/app/components/KaavyaUploader.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/components/KaavyaUploader.tsx contains `"use client"`, `extractTextFromPdf`, `saveKaavya`, `Save to Library`, `Drop a PDF here`, `Paste Sanskrit text here...`, drag-and-drop handlers
    - src/app/page.tsx contains `"use client"`, `KaavyaLibrary`, `KaavyaUploader`, view state with 'analyze' | 'library' | 'uploader' | 'reader', tab navigation with FaSearch and FaBook
    - src/app/layout.tsx contains "Sanskrit Learning Platform" in title
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Users can switch between Analyze and Library tabs. Library shows saved kaavyas. Uploader supports PDF drag-and-drop and text paste with title input. Saved kaavyas appear in library via reactive hook.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- All 3 new components exist with "use client" directive
- page.tsx has tab navigation between Analyze and Library views
- KaavyaUploader has both PDF upload and text paste modes
</verification>

<success_criteria>
- KaavyaLibrary renders responsive card grid with loading/empty/populated states
- LibraryCard shows title, author, reading progress, relative time, and delete with confirmation
- KaavyaUploader supports PDF drag-and-drop upload with text extraction and preview
- KaavyaUploader supports text paste with Shobhika font
- Page routing allows switching between Analyze, Library, Uploader, and (placeholder) Reader views
</success_criteria>

<output>
After completion, create `.planning/phases/08-kaavya-reader-and-storage-foundation/08-02-SUMMARY.md`
</output>
