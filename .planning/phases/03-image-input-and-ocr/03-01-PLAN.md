---
phase: 03-image-input-and-ocr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/analysis/ocr.ts
  - src/app/api/ocr/route.ts
  - src/lib/__tests__/ocr.test.ts
  - src/__tests__/ocr-api.test.ts
  - vitest.config.ts
autonomous: true
requirements: [INPUT-03]

must_haves:
  truths:
    - "Tesseract.js extracts Devanagari text from an image buffer server-side"
    - "POST /api/ocr accepts FormData with image file and returns extracted text"
    - "API validates file type (JPEG/PNG only) and file size (max 20MB)"
    - "Invalid requests return appropriate error responses (400)"
  artifacts:
    - path: "src/lib/analysis/ocr.ts"
      provides: "Tesseract.js OCR extraction function"
      exports: ["extractTextFromImage"]
    - path: "src/app/api/ocr/route.ts"
      provides: "POST endpoint for image OCR"
      exports: ["POST"]
    - path: "src/lib/__tests__/ocr.test.ts"
      provides: "Unit tests for OCR extraction (mocked Tesseract)"
    - path: "src/__tests__/ocr-api.test.ts"
      provides: "Unit tests for /api/ocr route validation"
  key_links:
    - from: "src/app/api/ocr/route.ts"
      to: "src/lib/analysis/ocr.ts"
      via: "import extractTextFromImage"
      pattern: "import.*extractTextFromImage.*from.*ocr"
    - from: "src/lib/analysis/ocr.ts"
      to: "tesseract.js"
      via: "createWorker with Devanagari traineddata"
      pattern: "createWorker.*Devanagari"
---

<objective>
OCR backend: Tesseract.js extraction function and /api/ocr API route with FormData handling and validation.

Purpose: Provide a server-side endpoint that accepts uploaded images and returns extracted Devanagari text using Tesseract.js (local OCR, no external API needed). This is the foundation that the frontend ImageUpload component will call.
Output: Working /api/ocr endpoint, tested with mocked Tesseract.js worker.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-image-input-and-ocr/03-RESEARCH.md
@src/lib/analysis/types.ts
@src/app/api/analyze/route.ts
@vitest.config.ts

<interfaces>
<!-- Existing patterns the executor needs -->

From src/app/api/analyze/route.ts (existing API route pattern):
```typescript
import { NextRequest, NextResponse } from "next/server";
// POST handler validates input, calls pipeline, returns JSON
export async function POST(request: NextRequest) { ... }
```

From vitest.config.ts (test config to update):
```typescript
environmentMatchGlobs: [
  ["src/__tests__/word-breakdown.test.tsx", "jsdom"],
],
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OCR extraction function with Tesseract.js</name>
  <files>src/lib/analysis/ocr.ts, src/lib/__tests__/ocr.test.ts</files>
  <behavior>
    - Test 1: extractTextFromImage returns extracted Devanagari text from image buffer
    - Test 2: extractTextFromImage trims whitespace from OCR output
    - Test 3: extractTextFromImage handles empty OCR result gracefully (returns empty string)
    - Test 4: extractTextFromImage throws on Tesseract worker failure
  </behavior>
  <action>
    Install tesseract.js: `npm install tesseract.js`

    Create `src/lib/analysis/ocr.ts` with a single exported function:

    ```typescript
    export async function extractTextFromImage(imageBuffer: Buffer): Promise<string>
    ```

    Implementation:
    - Import `createWorker` from `tesseract.js`
    - Create a worker with `'script/Devanagari'` traineddata (NOT `'san'` which is missing characters)
    - Call `worker.recognize(imageBuffer)` to get OCR result
    - Extract `result.data.text`, trim it
    - Call `worker.terminate()` to clean up (use try/finally to ensure cleanup)
    - Return the trimmed text

    For tests in `src/lib/__tests__/ocr.test.ts`:
    - Mock `tesseract.js` module using `vi.mock('tesseract.js')`
    - Mock `createWorker` to return a fake worker with `recognize` and `terminate` methods
    - The mock `recognize` should return `{ data: { text: 'धर्मक्षेत्रे कुरुक्षेत्रे\n' } }`
    - Test the function returns trimmed text
    - Test error propagation when worker.recognize throws
    - Test that worker.terminate is always called (even on error)
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npx vitest run src/lib/__tests__/ocr.test.ts -x</automated>
  </verify>
  <done>extractTextFromImage function works with mocked Tesseract.js, handles success/error/cleanup correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /api/ocr POST endpoint with FormData validation</name>
  <files>src/app/api/ocr/route.ts, src/__tests__/ocr-api.test.ts, vitest.config.ts</files>
  <behavior>
    - Test 1: Returns 400 when no image file in FormData
    - Test 2: Returns 400 when file type is not JPEG or PNG
    - Test 3: Returns 400 when file exceeds 20MB
    - Test 4: Returns 200 with extracted text on valid image upload
    - Test 5: Returns 500 with error message when OCR extraction fails
  </behavior>
  <action>
    Create `src/app/api/ocr/route.ts`:
    - Export async POST handler accepting NextRequest
    - Parse `request.formData()`, get `formData.get("image")` as File
    - Validate: file exists (400 if not), file.type in ["image/jpeg", "image/png"] (400 if not), file.size <= 20MB (400 if not)
    - Convert file to Buffer: `Buffer.from(await file.arrayBuffer())`
    - Call `extractTextFromImage(buffer)` from `@/lib/analysis/ocr`
    - Return `NextResponse.json({ text: extractedText })` on success
    - Wrap in try/catch, return 500 with `{ error: "OCR extraction failed" }` on failure

    Create `src/__tests__/ocr-api.test.ts`:
    - Mock `@/lib/analysis/ocr` module using `vi.mock`
    - Create helper to build mock NextRequest with FormData containing a File
    - Use `new File([new Uint8Array(10)], 'test.jpg', { type: 'image/jpeg' })` for valid files
    - Import the POST handler directly from the route module
    - Test all validation cases and success/error paths

    Update `vitest.config.ts`:
    - No changes needed for these tests (they run in node environment, not jsdom)
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npx vitest run src/__tests__/ocr-api.test.ts -x</automated>
  </verify>
  <done>POST /api/ocr validates file type/size, extracts text via Tesseract.js, returns JSON response. All 5 test cases pass.</done>
</task>

</tasks>

<verification>
All tests pass: `npx vitest run --reporter=verbose`
The /api/ocr endpoint is ready for the frontend ImageUpload component (Plan 02).
No Grok Vision or AI SDK imports in OCR code -- uses Tesseract.js only.
</verification>

<success_criteria>
- `npm install tesseract.js` completes successfully
- `extractTextFromImage` function uses Tesseract.js with `script/Devanagari` traineddata
- POST /api/ocr accepts FormData, validates file type and size, returns extracted text
- All OCR tests pass with mocked Tesseract.js worker
- Full test suite remains green: `npx vitest run --reporter=verbose`
</success_criteria>

<output>
After completion, create `.planning/phases/03-image-input-and-ocr/03-01-SUMMARY.md`
</output>
