---
phase: 03-image-input-and-ocr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/analysis/ocr.ts
  - src/app/api/ocr/route.ts
  - src/lib/__tests__/ocr.test.ts
  - src/__tests__/ocr-api.test.ts
  - vitest.config.ts
autonomous: true
requirements:
  - INPUT-03

must_haves:
  truths:
    - "OCR endpoint accepts an image and returns extracted Devanagari text"
    - "Invalid file types (not JPEG/PNG) are rejected with 400 error"
    - "Oversized files (>20MB) are rejected with 400 error"
    - "OCR prompt instructs Grok to treat text as Sanskrit, not Hindi"
  artifacts:
    - path: "src/lib/analysis/ocr.ts"
      provides: "extractTextFromImage function using Grok Vision"
      exports: ["extractTextFromImage"]
    - path: "src/app/api/ocr/route.ts"
      provides: "POST /api/ocr endpoint with FormData handling"
      exports: ["POST"]
    - path: "src/lib/__tests__/ocr.test.ts"
      provides: "Unit tests for OCR extraction with mocked Grok"
    - path: "src/__tests__/ocr-api.test.ts"
      provides: "API route tests for validation and error handling"
  key_links:
    - from: "src/app/api/ocr/route.ts"
      to: "src/lib/analysis/ocr.ts"
      via: "import extractTextFromImage"
      pattern: "extractTextFromImage"
    - from: "src/lib/analysis/ocr.ts"
      to: "@ai-sdk/xai"
      via: "generateText with xai vision model"
      pattern: 'xai\("grok-2-vision'
---

<objective>
Build the OCR backend: a Grok Vision extraction function and a /api/ocr API route that accepts image uploads via FormData and returns extracted Sanskrit text.

Purpose: INPUT-03 requires high-accuracy OCR of printed Devanagari. This plan creates the server-side infrastructure that the image upload UI (Plan 02) will call.
Output: `src/lib/analysis/ocr.ts`, `src/app/api/ocr/route.ts`, and associated tests.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-image-input-and-ocr/03-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From src/lib/analysis/pipeline.ts (existing LLM pattern):
```typescript
import { generateText } from "ai";
import { xai } from "@ai-sdk/xai";
// Uses Output.object() for structured output -- OCR uses plain generateText since output is raw text
```

From src/app/api/analyze/route.ts (existing API route pattern):
```typescript
import { NextRequest, NextResponse } from "next/server";
// POST handler pattern with try/catch and error JSON responses
```

From vitest.config.ts (current config):
```typescript
environmentMatchGlobs: [
  ["src/__tests__/word-breakdown.test.tsx", "jsdom"],
],
// OCR tests are node-only (no DOM), no jsdom needed for them
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OCR extraction function and tests</name>
  <files>src/lib/analysis/ocr.ts, src/lib/__tests__/ocr.test.ts</files>
  <behavior>
    - Test 1: extractTextFromImage returns Devanagari text from a base64 image (mocked Grok response)
    - Test 2: extractTextFromImage calls generateText with xai vision model and Sanskrit-specific prompt
    - Test 3: Prompt includes "Sanskrit (not Hindi)" instruction and preservation rules for visarga/anusvara
    - Test 4: Returned text is trimmed (no leading/trailing whitespace)
  </behavior>
  <action>
    Create `src/lib/__tests__/ocr.test.ts` with tests that mock `ai` module's generateText (same vi.mock pattern used in Phase 2 pipeline tests). Mock should return a sample Devanagari string (e.g. "धर्मक्षेत्रे कुरुक्षेत्रे").

    Then create `src/lib/analysis/ocr.ts` exporting `extractTextFromImage(imageBase64: string): Promise<string>`. Implementation:
    - Import generateText from "ai" and xai from "@ai-sdk/xai"
    - Use model `xai("grok-2-vision-1212")`
    - Send messages array with user role, content array containing text prompt and image content part
    - Text prompt must explicitly say: "This is Sanskrit text, NOT Hindi", preserve visarga/anusvara/conjuncts, preserve line breaks, return only Devanagari without translation
    - Return `text.trim()`

    Follow the exact code pattern from 03-RESEARCH.md "Complete OCR Flow (Server)" section.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npx vitest run src/lib/__tests__/ocr.test.ts -x</automated>
  </verify>
  <done>extractTextFromImage function exists, calls Grok Vision with Sanskrit-specific prompt, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OCR API route and tests</name>
  <files>src/app/api/ocr/route.ts, src/__tests__/ocr-api.test.ts</files>
  <behavior>
    - Test 1: POST with valid JPEG file returns 200 with { text: "..." }
    - Test 2: POST with no file returns 400 with { error: "No image provided" }
    - Test 3: POST with invalid MIME type (e.g. text/plain) returns 400 with error about supported formats
    - Test 4: POST with file exceeding 20MB returns 400 with size error
  </behavior>
  <action>
    Create `src/__tests__/ocr-api.test.ts` testing the POST handler directly by importing it. Mock `@/lib/analysis/ocr` to avoid real API calls. Create mock File/FormData objects for each test case. Use the same vi.mock pattern from Phase 2.

    Then create `src/app/api/ocr/route.ts` exporting a POST handler:
    1. Parse `request.formData()`, get `formData.get("image")` as File
    2. Validate file exists (400 if missing)
    3. Validate MIME type is image/jpeg or image/png (400 if invalid)
    4. Validate file size <= 20MB (400 if too large)
    5. Convert to base64: `Buffer.from(await file.arrayBuffer()).toString("base64")`
    6. Call `extractTextFromImage(base64)` from `@/lib/analysis/ocr`
    7. Return `NextResponse.json({ text: extractedText })`
    8. Wrap in try/catch, return 500 on unexpected errors

    Follow the exact pattern from 03-RESEARCH.md "FormData File Upload API Route" section.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npx vitest run src/__tests__/ocr-api.test.ts -x</automated>
  </verify>
  <done>POST /api/ocr validates inputs, calls OCR extraction, returns extracted text. All tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```
npx vitest run --reporter=verbose
```
All existing tests plus new OCR tests must pass.
</verification>

<success_criteria>
- extractTextFromImage function calls Grok Vision with Sanskrit-specific prompt
- POST /api/ocr accepts FormData image uploads and returns extracted text
- Invalid file types and oversized files are rejected with appropriate 400 errors
- All tests pass (mocked, no real API calls)
</success_criteria>

<output>
After completion, create `.planning/phases/03-image-input-and-ocr/03-01-SUMMARY.md`
</output>
