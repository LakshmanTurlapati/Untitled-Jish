---
phase: 03-image-input-and-ocr
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/components/AnalysisView.tsx
  - src/app/components/ImageUpload.tsx
  - src/__tests__/text-input.test.tsx
  - src/__tests__/image-upload.test.tsx
  - vitest.config.ts
autonomous: false
requirements:
  - INPUT-01
  - INPUT-02

must_haves:
  truths:
    - "User can type Devanagari text and see live IAST transliteration below the input"
    - "User can upload a JPEG or PNG image of printed Devanagari text"
    - "Uploaded image shows a preview before OCR extraction"
    - "Extracted OCR text populates the text input area for review before analysis"
    - "User sees loading state during OCR extraction"
    - "User sees error message if upload fails or file type is invalid"
  artifacts:
    - path: "src/app/components/AnalysisView.tsx"
      provides: "Enhanced text input with live IAST preview and ImageUpload integration"
      contains: "devanagariToIast"
    - path: "src/app/components/ImageUpload.tsx"
      provides: "Drag-drop/click image upload component with preview and OCR call"
      exports: ["ImageUpload"]
    - path: "src/__tests__/text-input.test.tsx"
      provides: "Tests for IAST transliteration preview display"
    - path: "src/__tests__/image-upload.test.tsx"
      provides: "Tests for image upload component behavior"
  key_links:
    - from: "src/app/components/ImageUpload.tsx"
      to: "/api/ocr"
      via: "fetch POST with FormData"
      pattern: "fetch.*api/ocr"
    - from: "src/app/components/AnalysisView.tsx"
      to: "src/app/components/ImageUpload.tsx"
      via: "onTextExtracted callback populates inputText state"
      pattern: "onTextExtracted"
    - from: "src/app/components/AnalysisView.tsx"
      to: "src/lib/transliteration.ts"
      via: "devanagariToIast for live IAST preview"
      pattern: "devanagariToIast"
---

<objective>
Add IAST transliteration preview to the text input and build an image upload component that extracts Devanagari text via OCR, populating the text input for user review before analysis.

Purpose: INPUT-01 requires live IAST display as users type Devanagari. INPUT-02 requires image upload with OCR extraction feeding into the existing analysis flow. Both paths converge on the same text input -> analysis pipeline.
Output: Enhanced `AnalysisView.tsx` with IAST preview, new `ImageUpload.tsx` component, and tests for both.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-image-input-and-ocr/03-RESEARCH.md
@.planning/phases/03-image-input-and-ocr/03-01-SUMMARY.md

<interfaces>
<!-- Existing contracts the executor needs -->

From src/app/components/AnalysisView.tsx (current implementation):
```typescript
"use client";
import { useState } from "react";
import type { EnrichedWord } from "@/lib/analysis/types";
import { WordBreakdown } from "./WordBreakdown";

export function AnalysisView() {
  const [inputText, setInputText] = useState("");
  const [analysisResult, setAnalysisResult] = useState<EnrichedWord[] | null>(null);
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // ... textarea + submit form + WordBreakdown grid
}
```

From src/lib/transliteration.ts:
```typescript
export function devanagariToIast(text: string): string;
export function iastToDevanagari(text: string): string;
```

From vitest.config.ts:
```typescript
environmentMatchGlobs: [
  ["src/__tests__/word-breakdown.test.tsx", "jsdom"],
],
// Must add new .tsx test files here for jsdom environment
```

From 03-RESEARCH.md -- ImageUpload component pattern:
```typescript
interface ImageUploadProps {
  onTextExtracted: (text: string) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IAST transliteration preview and ImageUpload component with tests</name>
  <files>src/app/components/AnalysisView.tsx, src/app/components/ImageUpload.tsx, src/__tests__/text-input.test.tsx, src/__tests__/image-upload.test.tsx, vitest.config.ts</files>
  <behavior>
    text-input.test.tsx:
    - Test 1: IAST transliteration preview is displayed below the textarea when Devanagari text is present
    - Test 2: IAST preview is empty/hidden when textarea is empty
    - Test 3: IAST preview updates when input text changes

    image-upload.test.tsx:
    - Test 1: File input accepts only JPEG and PNG (accept attribute)
    - Test 2: Uploading a file shows image preview
    - Test 3: Successful OCR calls onTextExtracted with extracted text
    - Test 4: Upload error displays error message
    - Test 5: Loading state shown during OCR extraction
  </behavior>
  <action>
    **Step 1: Update vitest.config.ts** -- Add the two new test files to environmentMatchGlobs:
    ```
    ["src/__tests__/text-input.test.tsx", "jsdom"],
    ["src/__tests__/image-upload.test.tsx", "jsdom"],
    ```

    **Step 2: Create test files (RED phase)**

    `src/__tests__/text-input.test.tsx`:
    - Import render, screen, fireEvent from @testing-library/react
    - Use `// @vitest-environment jsdom` directive at top
    - Import AnalysisView component
    - Mock `@/lib/transliteration` module -- make devanagariToIast return a predictable IAST string (e.g. input "dharma" for any input)
    - Test that typing Devanagari into the textarea causes an IAST preview element to appear with the transliterated text
    - Test that empty textarea shows no IAST preview text
    - Mock fetch to prevent actual API calls during form tests

    `src/__tests__/image-upload.test.tsx`:
    - Import render, screen, fireEvent, waitFor from @testing-library/react
    - Use `// @vitest-environment jsdom` directive at top
    - Import ImageUpload component
    - Mock global fetch for OCR API calls
    - Test file input has accept="image/jpeg,image/png"
    - Test that selecting a file triggers fetch to /api/ocr
    - Test onTextExtracted callback receives extracted text on success
    - Test error display on fetch failure
    - Test loading indicator during upload

    **Step 3: Implement components (GREEN phase)**

    Modify `src/app/components/AnalysisView.tsx`:
    - Import `devanagariToIast` from `@/lib/transliteration`
    - Import the new `ImageUpload` component
    - Compute `iastPreview = inputText ? devanagariToIast(inputText) : ""`
    - Add IAST preview below the textarea: a `<p>` with className "font-serif text-sm text-ink-600 italic" showing iastPreview, only rendered when inputText is non-empty. Add data-testid="iast-preview" for testing.
    - Add `ImageUpload` component above or below the textarea with `onTextExtracted={(text) => setInputText(text)}` callback that populates the textarea with OCR output
    - Keep all existing functionality (form submit, analysis results, error/loading states) unchanged

    Create `src/app/components/ImageUpload.tsx`:
    - "use client" directive
    - Props: `{ onTextExtracted: (text: string) => void }`
    - State: isUploading (boolean), preview (string | null), error (string | null)
    - handleFile function:
      1. Validate file.type is image/jpeg or image/png (set error if not)
      2. Validate file.size <= 20MB (set error if not)
      3. Create object URL for preview: `URL.createObjectURL(file)`
      4. Set isUploading true, clear error
      5. POST FormData with "image" field to /api/ocr (do NOT set Content-Type header)
      6. On success: call onTextExtracted(text)
      7. On error: set error message
      8. Finally: set isUploading false
    - Render:
      - A bordered upload zone with dashed border (border-dashed border-parchment-300)
      - Hidden file input triggered by clicking the zone
      - Upload icon/text prompt ("Upload an image of Sanskrit text")
      - Image preview when file selected (max-h-48 rounded)
      - Loading spinner/text during extraction ("Extracting text...")
      - Error message in red if error
    - Support drag-and-drop: onDragOver (preventDefault), onDrop (get file from dataTransfer)

    Style using existing design tokens: parchment-*, ink-*, accent-* palette.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npx vitest run src/__tests__/text-input.test.tsx src/__tests__/image-upload.test.tsx -x</automated>
  </verify>
  <done>IAST preview shows below textarea when Devanagari is typed. ImageUpload component handles file selection, validates types, shows preview, calls /api/ocr, and populates text input via callback. All tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete input and OCR flow end-to-end</name>
  <action>Human verifies the complete image input and OCR pipeline visually and functionally in the browser.</action>
  <what-built>Complete image input and OCR flow: (1) Text input with live IAST transliteration preview, (2) Image upload with drag-drop and preview, (3) OCR extraction populating the text area, (4) Full pipeline from image upload through analysis results display</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Visit http://localhost:3000
    3. **Test INPUT-01 (text input + IAST)**:
       a. Type or paste Devanagari text (e.g. "dharmakShetre kurukShetre") into the textarea
       b. Verify IAST transliteration appears below the textarea in italic text
       c. Clear the textarea -- verify IAST preview disappears
       d. Click "Analyze" -- verify word breakdown results appear as before
    4. **Test INPUT-02 + INPUT-03 (image upload + OCR)**:
       a. Find or create a photo of printed Sanskrit text (e.g. Bhagavad Gita verse)
       b. Upload via the image upload area (click or drag-drop)
       c. Verify image preview appears
       d. Verify "Extracting text..." loading state shows
       e. Verify extracted Devanagari text populates the text input area
       f. Review the extracted text for accuracy
       g. Click "Analyze" to run full pipeline on the OCR output
       h. Verify word breakdown results appear
    5. **Test error handling**:
       a. Try uploading a non-image file (e.g. .txt) -- verify error message
  </how-to-verify>
  <verify>Human confirms all flows work correctly</verify>
  <done>User approves text input with IAST preview, image upload with OCR extraction, and full pipeline integration</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```
npx vitest run --reporter=verbose
```
All existing tests (88 from Phase 2) plus new text-input and image-upload tests must pass.
</verification>

<success_criteria>
- Typing Devanagari shows live IAST transliteration preview below the textarea
- Image upload component accepts JPEG/PNG via click or drag-drop
- Uploaded image shows preview before OCR runs
- OCR-extracted text populates the text input for user review
- User can then click "Analyze" to run the full analysis pipeline
- All tests pass including new component tests
- Human verification approves the complete flow
</success_criteria>

<output>
After completion, create `.planning/phases/03-image-input-and-ocr/03-02-SUMMARY.md`
</output>
