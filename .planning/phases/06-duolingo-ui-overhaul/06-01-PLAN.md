---
phase: 06-duolingo-ui-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/page.tsx
  - src/app/components/AnalysisView.tsx
  - src/app/components/ImageUpload.tsx
  - src/__tests__/text-input.test.tsx
  - src/__tests__/app-shell.test.ts
  - vitest.config.ts
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Page has minimal header with just 'Sanskrit Analyzer' title, no subtitle, no sample verse"
    - "Input area is a single hero card containing textarea, IAST preview, and image upload together"
    - "Sticky bottom bar holds the primary action button (Analyze), always visible"
    - "After analysis completes, tabbed navigation shows Words | Vocabulary | Quiz pills"
    - "During analysis loading, step-by-step progress displays with checkmarks"
    - "All primary action buttons use 3D pressed effect (border-b-4, active shrink)"
    - "Layout is centered single column at ~640px max-width"
  artifacts:
    - path: "src/app/globals.css"
      provides: "New CSS variables (accent-800), confetti/check-appear/fade-in keyframes"
      contains: "accent-800"
    - path: "src/app/page.tsx"
      provides: "Minimal header, centered single-column layout"
    - path: "src/app/components/AnalysisView.tsx"
      provides: "Hero input card, tab bar, sticky bottom bar, analysis progress steps"
      exports: ["AnalysisView"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/components/AnalysisView.tsx"
      via: "import and render"
      pattern: "import.*AnalysisView"
    - from: "src/app/components/AnalysisView.tsx"
      to: "src/app/components/WordBreakdown.tsx"
      via: "tab rendering (words tab)"
      pattern: "WordBreakdown"
    - from: "src/app/components/AnalysisView.tsx"
      to: "src/app/components/QuizView.tsx"
      via: "tab rendering (quiz tab)"
      pattern: "QuizView"
---

<objective>
Restructure the page layout and AnalysisView component to implement Duolingo-inspired layout patterns: minimal header, hero input card, tabbed sections, sticky bottom action bar, analysis progress steps, 3D buttons, and centered single-column layout.

Purpose: This is the structural foundation that the component restyling (Plan 02) and quiz gamification (Plan 03) render inside. Establishes the new layout patterns, CSS variables, and keyframe animations.
Output: Restructured page.tsx and AnalysisView.tsx with new Duolingo-style layout, globals.css with new tokens and animations.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-duolingo-ui-overhaul/06-CONTEXT.md
@.planning/phases/06-duolingo-ui-overhaul/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/analysis/types.ts:
```typescript
export interface EnrichedWord extends AnalyzedWord {
  mw_definitions: string[];
  apte_definitions: string[];
  meaning_source: MeaningSource;
}
```

From src/app/components/ImageUpload.tsx:
```typescript
interface ImageUploadProps {
  onTextExtracted: (text: string) => void;
}
export function ImageUpload({ onTextExtracted }: ImageUploadProps)
```

From src/app/components/WordBreakdown.tsx:
```typescript
interface WordBreakdownProps { word: EnrichedWord; }
export function WordBreakdown({ word }: WordBreakdownProps)
```

From src/app/components/VocabularyList.tsx:
```typescript
interface VocabularyListProps { words: EnrichedWord[]; }
export function VocabularyList({ words }: VocabularyListProps)
```

From src/app/components/QuizView.tsx:
```typescript
interface QuizViewProps { words: EnrichedWord[]; }
export function QuizView({ words }: QuizViewProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS variables and keyframe animations to globals.css</name>
  <files>src/app/globals.css</files>
  <action>
Add to the existing `@theme` block in globals.css:
- `--color-accent-800: #78350f;` (darker shade for 3D button borders)

Add after the `@theme` block these keyframe animations:
- `confetti-fall`: translateY(-10vh) rotate(0deg) opacity(1) to translateY(100vh) rotate(720deg) opacity(0)
- `check-appear`: scale(0) opacity(0) to scale(1.2) to scale(1) opacity(1)
- `fade-in`: opacity(0) translateY(4px) to opacity(1) translateY(0)

Preserve ALL existing CSS exactly as-is. Only add new content.
  </action>
  <verify>
    <automated>grep -c "accent-800\|confetti-fall\|check-appear\|fade-in" src/app/globals.css | grep -q "4" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>globals.css has accent-800 token and 3 keyframe animations (confetti-fall, check-appear, fade-in)</done>
</task>

<task type="auto">
  <name>Task 2: Restructure page.tsx and AnalysisView.tsx with Duolingo layout</name>
  <files>src/app/page.tsx, src/app/components/AnalysisView.tsx, src/app/components/ImageUpload.tsx, src/__tests__/text-input.test.tsx, src/__tests__/app-shell.test.ts, vitest.config.ts</files>
  <action>
**page.tsx** - Simplify to minimal header + AnalysisView:
- Remove the subtitle paragraph ("Deep grammatical analysis...")
- Remove the entire "Sample Verse" blockquote section and the hr divider
- Remove the "Text Analysis" heading and description paragraph
- Keep only: `<h1>Sanskrit Analyzer</h1>` centered, then `<AnalysisView />`
- Set main container to centered single column: `max-w-[640px] mx-auto px-4 py-8 pb-24` (pb-24 for sticky bar clearance)
- Keep the h1 styling clean: `text-3xl font-bold text-ink-900 text-center mb-6`

**AnalysisView.tsx** - Major restructure:

1. **Hero Input Card**: Wrap textarea, IAST preview, and ImageUpload in a single big rounded card:
   - `rounded-2xl border border-parchment-200 bg-parchment-50 p-6` (16-20px border-radius = rounded-2xl)
   - Textarea inside the card, IAST preview below it, then ImageUpload below that
   - Remove the old "Upload Image" label and separate section for ImageUpload
   - Remove the old inline Analyze button from the form (it moves to sticky bar)

2. **Sticky Bottom Action Bar**: Fixed bottom bar with the primary action button:
   - `fixed bottom-0 left-0 right-0 bg-parchment-50/95 backdrop-blur-sm border-t border-parchment-200 p-4 z-50`
   - Inner container: `mx-auto max-w-[640px]`
   - Button: full-width 3D pressed effect: `w-full rounded-xl bg-accent-600 text-white font-bold px-8 py-3 border-b-4 border-accent-800 active:border-b-2 active:translate-y-[2px] transition-all duration-75 disabled:opacity-50 disabled:active:translate-y-0 disabled:active:border-b-4`
   - Button label changes by context: "Analyze" when input has text, disabled when empty or loading
   - The button triggers the existing handleSubmit logic (convert form onSubmit to button onClick)

3. **Analysis Progress Steps** (shown during loading, replaces "Analyzing Sanskrit text..."):
   - Three steps: "Splitting sandhi...", "Analyzing morphology...", "Looking up meanings..."
   - Use `useState` for `completedSteps` (number 0-3) and `useEffect` with timers:
     - Step 1 completes at 800ms, step 2 at 2000ms, step 3 at 3500ms (via setTimeout)
     - When API response arrives (isLoading becomes false), immediately set all steps to 3 (complete)
     - Clear timeouts on cleanup and when loading stops
   - Each step: checkmark icon (green, with check-appear animation) when complete, spinner (animate-spin) when current, gray when pending
   - Display in a card with `rounded-2xl border border-parchment-200 bg-parchment-50 p-6`

4. **Tab Navigation** (shown when analysisResult exists and has words):
   - `useState<'words' | 'vocabulary' | 'quiz'>('words')` for active tab
   - Tab bar: `flex gap-2 justify-center mb-6`
   - Each tab button: pill style with `rounded-full px-5 py-2 text-sm font-semibold transition-colors`
   - Active: `bg-accent-600 text-white`
   - Inactive: `text-ink-700 hover:bg-parchment-200`
   - Tab labels: "Words", "Vocabulary", "Quiz"
   - Conditional rendering based on activeTab:
     - 'words': render WordBreakdown cards in single column (change grid from `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` to just `space-y-4` single column)
     - 'vocabulary': render `<VocabularyList words={analysisResult} />`
     - 'quiz': render `<QuizView words={analysisResult} />`

5. **Error state**: Keep existing error card but style with rounded-2xl to match new cards.

6. **Empty results state**: Keep but match rounded-2xl styling.

**ImageUpload.tsx** - Minor adjustment:
- Remove the outer wrapping div if it adds unwanted spacing
- The drag-drop zone text and styling stays the same, it now lives inside the hero card context
- Keep all existing functionality (handleFile, handleDrop, preview, error, loading state)

**Update tests:**

**src/__tests__/app-shell.test.ts**:
- Update "contains the app title" test: still checks for "Sanskrit Analyzer" (unchanged)
- Remove or update "contains sample Devanagari text" test: page.tsx no longer has the sample verse. Change to verify that the page does NOT contain the sample verse blockquote text, or simply remove this test since the Devanagari text now comes from AnalysisView's textarea (dynamic)
- Keep "does not import auth" test unchanged
- Keep "exports a default function component" unchanged

**src/__tests__/text-input.test.tsx**:
- Update mocks: AnalysisView now renders ImageUpload inside the hero card, VocabularyList and QuizView also imported — mock them:
  - Add mock for VocabularyList: `vi.mock("@/app/components/VocabularyList", () => ({ VocabularyList: () => <div data-testid="mock-vocab" /> }))`
  - Add mock for QuizView: `vi.mock("@/app/components/QuizView", () => ({ QuizView: () => <div data-testid="mock-quiz" /> }))`
  - Add mock for WordBreakdown: `vi.mock("@/app/components/WordBreakdown", () => ({ WordBreakdown: () => <div data-testid="mock-word" /> }))`
- The textarea placeholder stays "Enter Sanskrit text in Devanagari..." so existing tests should still find it
- Update the "does not show IAST preview" test: it currently queries by CSS class `.italic.text-ink-600` — instead query by absence of transliterated text
- The Analyze button is now in the sticky bar, not inside the form — update if any test looks for it by form association

**vitest.config.ts**: No changes needed since text-input.test.tsx already uses `@vitest-environment jsdom` directive inline. But verify the environmentMatchGlobs still work.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/app-shell.test.ts src/__tests__/text-input.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
- page.tsx shows only "Sanskrit Analyzer" title, no subtitle, no sample verse
- AnalysisView has hero input card (textarea + IAST + ImageUpload in one rounded-2xl card)
- Sticky bottom bar with 3D Analyze button always visible
- Analysis progress steps display during loading with timed checkmarks
- Tab bar (Words | Vocabulary | Quiz) appears after analysis with pill-style switching
- Word cards render in single column (no multi-column grid)
- All existing tests pass with necessary updates
  </done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — full test suite passes
- Visual: page shows minimal header, hero input card, sticky bottom bar
- Visual: tabs switch between Words/Vocabulary/Quiz views
- Visual: 3D button press effect works on Analyze button
</verification>

<success_criteria>
- Minimal header with title only (no subtitle, no sample verse)
- Hero input card contains textarea + IAST preview + ImageUpload
- Sticky bottom bar with 3D-effect Analyze button
- Tab navigation (Words | Vocabulary | Quiz) with pill styling
- Analysis progress steps with timed checkmarks during loading
- Single column layout at ~640px max-width
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-duolingo-ui-overhaul/06-01-SUMMARY.md`
</output>
