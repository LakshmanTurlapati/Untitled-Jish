---
phase: 01-foundation-and-text-input
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - INPUT-01
  - ANAL-05
  - UI-01
  - UI-03

must_haves:
  truths:
    - "ROADMAP Phase 1 success criteria do not reference text input UI (deferred to Phase 3)"
    - "REQUIREMENTS.md INPUT-01 is marked as Pending and mapped to Phase 3, not Phase 1"
    - "ROADMAP Phase 3 success criteria include text input alongside image upload"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 1 and Phase 3 success criteria"
      contains: "text input"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected INPUT-01 status and phase mapping"
      contains: "INPUT-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "INPUT-01 phase mapping matches ROADMAP phase assignments"
      pattern: "INPUT-01.*Phase 3"
---

<objective>
Fix documentation inconsistency: ROADMAP success criteria and REQUIREMENTS.md claim Phase 1 delivers text input UI, but the user explicitly deferred INPUT-01 to Phase 3 during discuss-phase. Update both documents to reflect this conscious decision.

Purpose: Eliminate false verification failures caused by docs contradicting the actual plan scope
Output: Updated ROADMAP.md and REQUIREMENTS.md with correct phase assignments
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-text-input/01-CONTEXT.md
@.planning/phases/01-foundation-and-text-input/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md Phase 1 and Phase 3 success criteria</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit ROADMAP.md to fix the Phase 1 / Phase 3 success criteria mismatch:

Phase 1 Success Criteria changes:
- Remove criterion 1 ("User can paste or type Devanagari text into the app and see it rendered with proper typography") -- this is Phase 3 work per user decision in 01-CONTEXT.md
- Remove criterion 2 ("User sees accurate IAST transliteration for each word in entered text") -- depends on text input which is Phase 3
- Replace with criteria that reflect what Phase 1 actually delivers:
  1. "Bidirectional IAST transliteration engine converts Devanagari text accurately (tested programmatically)"
  2. "App shell renders with Shobhika Devanagari typography and warm academic design tokens"
- Keep existing criteria 3 and 4 (no login, dictionary retrieval) as they are correct

Phase 1 Requirements line:
- Remove INPUT-01 from the Requirements list (change from "INPUT-01, ANAL-05, UI-01, UI-03" to "ANAL-05, UI-01, UI-03")

Phase 3 changes:
- Add INPUT-01 to Phase 3 Requirements list (change from "INPUT-02, INPUT-03" to "INPUT-01, INPUT-02, INPUT-03")
- Add a success criterion: "User can paste or type Devanagari text into the app and see it rendered with proper typography, with IAST transliteration displayed"

Do NOT change Phase 1 status (it remains Complete) or any other phase content.
  </action>
  <verify>
    <automated>grep -c "INPUT-01" .planning/ROADMAP.md | grep -q "1" && grep "Phase 1" .planning/ROADMAP.md | grep -v "INPUT-01" > /dev/null && grep "Phase 3" .planning/ROADMAP.md | head -5 | grep -q "INPUT-01" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Phase 1 success criteria reflect infrastructure-only delivery (no text input UI). Phase 3 success criteria include text input. INPUT-01 appears only under Phase 3, not Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md INPUT-01 status and phase mapping</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit REQUIREMENTS.md to fix the INPUT-01 inconsistency:

1. In the "Image and Input" section, change INPUT-01 from:
   `- [x] **INPUT-01**: User can paste or type Devanagari text directly into the app`
   to:
   `- [ ] **INPUT-01**: User can paste or type Devanagari text directly into the app`
   (unchecked -- it is NOT complete, it was deferred)

2. In the Traceability table, change INPUT-01 row from:
   `| INPUT-01 | Phase 1 | Complete |`
   to:
   `| INPUT-01 | Phase 3 | Pending |`

3. Update the Coverage section if needed to reflect the change (requirement count stays the same, just the mapping changes).

Do NOT change any other requirement statuses or mappings.
  </action>
  <verify>
    <automated>grep "INPUT-01" .planning/REQUIREMENTS.md | grep -q "\[ \]" && grep "INPUT-01" .planning/REQUIREMENTS.md | grep -q "Phase 3" && grep "INPUT-01" .planning/REQUIREMENTS.md | grep -q "Pending" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>INPUT-01 is marked as unchecked (pending), mapped to Phase 3 in the traceability table, and no longer falsely claims completion.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. ROADMAP.md Phase 1 success criteria make no reference to text input UI or user-entered text
2. ROADMAP.md Phase 3 includes INPUT-01 in its requirements and text input in success criteria
3. REQUIREMENTS.md INPUT-01 is `[ ]` (not `[x]`) and mapped to Phase 3
4. No other requirements or phases are affected
5. The documentation now accurately reflects the user's decision to defer text input to Phase 3
</verification>

<success_criteria>
- ROADMAP Phase 1 success criteria match actual deliverables (transliteration engine, typography, dictionary, no-auth)
- ROADMAP Phase 3 requirements include INPUT-01
- REQUIREMENTS.md INPUT-01 status is Pending, mapped to Phase 3
- Re-running verification against updated criteria would produce 0 gaps
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-text-input/01-03-SUMMARY.md`
</output>
