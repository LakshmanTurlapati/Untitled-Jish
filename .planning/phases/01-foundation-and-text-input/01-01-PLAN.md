---
phase: 01-foundation-and-text-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/fonts.ts
  - src/lib/transliteration.ts
  - src/lib/__tests__/transliteration.test.ts
  - src/__tests__/app-shell.test.ts
  - public/fonts/shobhika/Shobhika-Regular.woff2
  - public/fonts/shobhika/Shobhika-Bold.woff2
autonomous: true
requirements: [INPUT-01, ANAL-05, UI-01, UI-03]

must_haves:
  truths:
    - "IAST transliteration converts Devanagari text accurately and bidirectionally"
    - "App loads without any login or authentication gate"
    - "Devanagari text renders with Shobhika font and proper ligatures"
    - "Design tokens establish a warm academic aesthetic with parchment/ink palette"
  artifacts:
    - path: "src/lib/transliteration.ts"
      provides: "Bidirectional Devanagari/IAST and SLP1 conversion utility"
      exports: ["devanagariToIast", "iastToDevanagari", "slp1ToDevanagari", "slp1ToIast"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with Shobhika font and design tokens applied"
      contains: "shobhika"
    - path: "src/app/page.tsx"
      provides: "App shell landing page"
      min_lines: 20
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "vitest"
    - path: "src/lib/fonts.ts"
      provides: "Shobhika font configuration via next/font/local"
      exports: ["shobhika"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/lib/fonts.ts"
      via: "font variable import"
      pattern: "import.*fonts"
    - from: "src/app/globals.css"
      to: "Tailwind v4 theme"
      via: "@theme block with design tokens"
      pattern: "@theme"
---

<objective>
Scaffold the Next.js project, set up test infrastructure, visual foundation (Shobhika font + Tailwind design tokens), bidirectional IAST transliteration utility, and basic app shell.

Purpose: Establishes the entire project foundation -- every subsequent plan and phase builds on this scaffold. The transliteration utility is a core building block used across all phases.
Output: Working Next.js app with Vitest tests, Shobhika Devanagari typography, design system, and tested transliteration module.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-text-input/01-CONTEXT.md
@.planning/phases/01-foundation-and-text-input/01-RESEARCH.md
@.planning/phases/01-foundation-and-text-input/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with test infra and visual foundation</name>
  <files>
    package.json, tsconfig.json, vitest.config.ts, next.config.ts,
    src/app/layout.tsx, src/app/globals.css, src/lib/fonts.ts,
    public/fonts/shobhika/Shobhika-Regular.woff2, public/fonts/shobhika/Shobhika-Bold.woff2
  </files>
  <action>
    1. Run `npx create-next-app@latest sanskrit-analyzer --yes` in the project root. If the tool creates a subdirectory, move all contents up to the project root (the project root IS the app root).

    2. Install all Phase 1 dependencies:
       ```
       npm install better-sqlite3 @indic-transliteration/sanscript
       npm install -D @types/better-sqlite3 vitest @vitejs/plugin-react
       ```

    3. Create `vitest.config.ts` at project root:
       - Import `@vitejs/plugin-react`
       - Set test.globals: true, test.environment: 'node'
       - Add path aliases matching tsconfig (@ -> src/)
       - Add "test" script to package.json: `"test": "vitest run --reporter=verbose"`

    4. Download Shobhika font files from the GitHub release (https://github.com/Sandhi-IITBombay/Shobhika/releases):
       - Download Shobhika-Regular.otf and Shobhika-Bold.otf
       - Convert to WOFF2 format. Use a Node.js approach: install `wawoff2` as a dev dependency if available, or use an alternative WOFF2 conversion tool. If no CLI tool works cleanly, download pre-converted WOFF2 files or use the OTF files directly with next/font/local (which handles OTF natively).
       - Place font files in `public/fonts/shobhika/`

    5. Create `src/lib/fonts.ts`:
       - Use `next/font/local` to load Shobhika Regular (400) and Bold (700)
       - Set `variable: '--font-shobhika'` and `display: 'swap'`
       - Export the font object as `shobhika`
       - Adjust the `src` paths to be relative from the fonts.ts file location to public/fonts/shobhika/

    6. Create `src/app/globals.css` with Tailwind v4 CSS-first configuration:
       - `@import "tailwindcss";`
       - Define `@theme` block with:
         - `--font-sans`: system defaults
         - `--font-sanskrit`: var(--font-shobhika), 'Noto Sans Devanagari', sans-serif
         - Warm academic color palette: parchment-50 (#fdf8f0), parchment-100 (#f9edd8), parchment-200 (#f3dbb1), ink-700 (#3d2e1f), ink-800 (#2a1f14), ink-900 (#1a1209), accent-500 (#b45309), accent-600 (#92400e)
         - Sanskrit line-height spacing: --spacing-sanskrit: 1.8

    7. Update `src/app/layout.tsx`:
       - Import shobhika from `@/lib/fonts`
       - Apply `shobhika.variable` to the html element className
       - Set metadata: title "Sanskrit Analyzer", description about Sanskrit text analysis
       - Use a clean body with parchment background color

    INPUT-01 note: Text input UI is DEFERRED to Phase 3 per user decision. No text input component in this phase. This plan acknowledges INPUT-01 in requirements but performs no work for it.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js app builds successfully. Shobhika font files exist in public/fonts/shobhika/. Vitest config exists and `npm test` runs (even if no tests yet). globals.css has Tailwind v4 @theme block with design tokens. Root layout applies Shobhika font variable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transliteration utility and app shell with tests</name>
  <files>
    src/lib/transliteration.ts, src/lib/__tests__/transliteration.test.ts,
    src/app/page.tsx, src/__tests__/app-shell.test.ts
  </files>
  <behavior>
    - devanagariToIast("नमस्ते") returns "namaste"
    - iastToDevanagari("namaste") returns "नमस्ते"
    - devanagariToIast("धर्म") returns "dharma"
    - Round-trip: iastToDevanagari(devanagariToIast(input)) === input for standard text
    - slp1ToIast("Darma") converts SLP1 to correct IAST "dharma" (note SLP1 D = IAST dh)
    - slp1ToDevanagari converts SLP1 to Devanagari correctly
    - Empty string input returns empty string (edge case)
    - App shell page renders without requiring authentication
    - App shell page contains the app title
  </behavior>
  <action>
    1. Create `src/lib/__tests__/transliteration.test.ts` FIRST (TDD red phase):
       - Test devanagariToIast with known Devanagari words (नमस्ते -> namaste, धर्म -> dharma, संस्कृतम् -> sanskrtam)
       - Test iastToDevanagari with IAST strings
       - Test round-trip fidelity: convert to IAST then back to Devanagari, verify match
       - Test slp1ToIast with SLP1 input (CDSL uses SLP1 internally)
       - Test slp1ToDevanagari
       - Test edge cases: empty string, single characters, text with spaces
       - Run tests: they MUST fail (module doesn't exist yet)

    2. Create `src/lib/transliteration.ts` (TDD green phase):
       - Import Sanscript from '@indic-transliteration/sanscript'
       - Export four functions: devanagariToIast, iastToDevanagari, slp1ToDevanagari, slp1ToIast
       - Each wraps Sanscript.t() with the correct scheme names: 'devanagari', 'iast', 'slp1'
       - Run tests: they MUST pass

    3. Create `src/__tests__/app-shell.test.ts` FIRST (TDD red phase):
       - Smoke test: import page component module, verify it exports a default function
       - No auth test: verify the module does not import or reference any auth/login/session modules
       - Run test: MUST fail

    4. Create/update `src/app/page.tsx` (TDD green phase):
       - Basic app shell landing page with:
         - App title "Sanskrit Analyzer" in a header
         - Subtitle describing the tool's purpose
         - A sample Devanagari text block (e.g., a well-known verse) styled with font-sanskrit class to demonstrate Shobhika rendering
         - Placeholder area where future analysis results will appear
         - Use Tailwind classes with the parchment/ink design tokens
       - No login, no auth, no user state -- just a static page
       - Run tests: MUST pass

    5. Run full test suite: `npm test`
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npm test</automated>
  </verify>
  <done>All transliteration tests pass (Devanagari-to-IAST, IAST-to-Devanagari, SLP1 conversions, round-trip, edge cases). App shell smoke test passes confirming no auth requirement. App shell page renders with Sanskrit title and sample Devanagari text.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm test` passes all transliteration and smoke tests
3. Shobhika font files exist in public/fonts/shobhika/
4. Design tokens defined in globals.css @theme block
5. App shell page displays without login gate
</verification>

<success_criteria>
- Vitest runs with `npm test` and all tests pass
- Next.js build succeeds with `npm run build`
- Transliteration module correctly converts between Devanagari, IAST, and SLP1
- Root layout applies Shobhika font variable
- App shell page is accessible without authentication
- Design tokens establish the warm academic color palette
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-text-input/01-01-SUMMARY.md`
</output>
