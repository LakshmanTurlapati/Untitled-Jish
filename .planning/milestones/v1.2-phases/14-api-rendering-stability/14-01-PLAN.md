---
phase: 14-api-rendering-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/api/env.ts
  - src/app/api/hints/route.ts
  - src/app/api/analyze/route.ts
  - src/app/api/quiz/populate/route.ts
  - src/lib/analysis/pipeline.ts
autonomous: true
requirements: [API-01, API-02, API-03, API-04]

must_haves:
  truths:
    - "Starting the app without XAI_API_KEY produces a clear error message naming the missing variable"
    - "When any API route throws, the error is logged with request context before the 500 response is sent"
    - "The /api/hints route awaits streamText so errors before stream start return JSON, not broken SSE"
    - "When analyzeText receives null LLM output, the error message identifies that the schema validation failed"
  artifacts:
    - path: "src/lib/api/env.ts"
      provides: "Env validation utility"
      exports: ["requireEnv"]
    - path: "src/app/api/hints/route.ts"
      provides: "Fixed streaming with await and error logging"
      contains: "await streamText"
    - path: "src/app/api/analyze/route.ts"
      provides: "Error logging with request context"
      contains: "console.error"
    - path: "src/app/api/quiz/populate/route.ts"
      provides: "Error logging with request context"
      contains: "console.error"
    - path: "src/lib/analysis/pipeline.ts"
      provides: "Env validation call and improved error message"
      contains: "requireEnv"
  key_links:
    - from: "src/lib/analysis/pipeline.ts"
      to: "src/lib/api/env.ts"
      via: "import requireEnv"
      pattern: "requireEnv.*XAI_API_KEY"
    - from: "src/app/api/hints/route.ts"
      to: "src/lib/api/env.ts"
      via: "import requireEnv"
      pattern: "requireEnv.*XAI_API_KEY"
---

<objective>
Fix API error handling across all routes -- add environment variable validation, structured error logging with request context, proper streaming error handling, and improved LLM validation error messages.

Purpose: API routes currently return generic 500 errors without logging, the hints endpoint does not await streamText (masking errors), and missing XAI_API_KEY causes opaque runtime failures instead of clear startup errors.

Output: All API routes log errors with context, env vars are validated before use, streaming errors return proper JSON, and LLM output validation errors are actionable.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-api-rendering-stability/14-CONTEXT.md

@src/app/api/hints/route.ts
@src/app/api/analyze/route.ts
@src/app/api/quiz/populate/route.ts
@src/lib/analysis/pipeline.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/analysis/pipeline.ts:
```typescript
export async function analyzeText(text: string): Promise<AnalysisResult>
// Uses: xai("grok-4-1-fast-non-reasoning"), generateText with Output.object
```

From src/app/api/hints/route.ts:
```typescript
// Uses: xai("grok-3-mini"), streamText, result.toTextStreamResponse()
// Currently: streamText is NOT awaited -- errors are swallowed
```

Established error response pattern:
```typescript
return NextResponse.json(
  { error: "user message", message: "technical detail" },
  { status: 500 }
);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create env validation utility and wire into API routes</name>
  <files>src/lib/api/env.ts, src/app/api/hints/route.ts, src/lib/analysis/pipeline.ts</files>
  <read_first>
    - src/app/api/hints/route.ts (current streaming pattern without await)
    - src/lib/analysis/pipeline.ts (current xai() call without env check)
  </read_first>
  <action>
    1. Create src/lib/api/env.ts with a requireEnv(name: string): string function that reads process.env[name], and if falsy, throws an Error with message: `"${name} environment variable is not set. Set it in .env.local or your deployment environment."` (per API-01).

    2. In src/lib/analysis/pipeline.ts, import requireEnv from "@/lib/api/env". At the top of analyzeText(), call `requireEnv("XAI_API_KEY")` before the generateText call. This ensures a clear error before any LLM call. Also improve the null output error message (per API-04): change `"Failed to generate analysis: LLM returned no output"` to `"LLM analysis failed: structured output was null. The model may have returned content that did not match the expected schema (FullAnalysisSchema). Check the prompt and schema compatibility."`.

    3. In src/app/api/hints/route.ts, import requireEnv from "@/lib/api/env". Call `requireEnv("XAI_API_KEY")` at the start of the POST handler (inside try, before input validation). Add `await` to the streamText() call on line 58 so errors from model initialization are caught by the outer try/catch instead of creating a broken stream (per API-03). In the catch block, add `console.error("Hints API error:", { error: error instanceof Error ? error.message : String(error), stack: error instanceof Error ? error.stack : undefined });` before the existing NextResponse.json return (per API-02).
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Untitled\ Jish && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/api/env.ts exists and exports requireEnv
    - pipeline.ts calls requireEnv("XAI_API_KEY") before generateText
    - pipeline.ts has improved null output error message mentioning schema
    - hints/route.ts calls requireEnv("XAI_API_KEY") at handler start
    - hints/route.ts has `await` before streamText()
    - hints/route.ts catch block logs error with console.error before returning JSON
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Env validation catches missing XAI_API_KEY with clear message; hints route awaits streamText and logs errors; LLM null output error is actionable</done>
</task>

<task type="auto">
  <name>Task 2: Add structured error logging to remaining API routes</name>
  <files>src/app/api/analyze/route.ts, src/app/api/quiz/populate/route.ts</files>
  <read_first>
    - src/app/api/analyze/route.ts (current catch block)
    - src/app/api/quiz/populate/route.ts (current catch block)
    - src/app/api/dictionary/route.ts (already has console.error -- reference pattern)
    - src/app/api/ocr/route.ts (already has console.error -- reference pattern)
  </read_first>
  <action>
    1. In src/app/api/analyze/route.ts (per API-02): In the catch block, add `console.error("Analyze API error:", { error: error instanceof Error ? error.message : String(error), stack: error instanceof Error ? error.stack : undefined, textLength: typeof text === "string" ? text.length : "invalid" });` before the existing NextResponse.json. Also update the response to include `detail` field: change `{ error: "Analysis failed", message }` to `{ error: "Analysis failed", detail: message }` for consistency with the decision in CONTEXT.md.

    2. In src/app/api/quiz/populate/route.ts (per API-02): In the outer catch block (line 80-93), add `console.error("Quiz populate API error:", { error: error instanceof Error ? error.message : String(error), stack: error instanceof Error ? error.stack : undefined });` before the existing return statements. Also update the 500 response to use `detail` instead of `message`: change `{ error: "Analysis failed", message }` to `{ error: "Analysis failed", detail: message }`.

    Note: dictionary/route.ts, distractors/route.ts, and ocr/route.ts already have console.error logging -- leave them as-is.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Untitled\ Jish && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - analyze/route.ts catch block has console.error with error message, stack, and textLength context
    - analyze/route.ts error response uses { error, detail } format
    - quiz/populate/route.ts outer catch block has console.error with error and stack context
    - quiz/populate/route.ts error response uses { error, detail } format
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All API routes log errors with context before returning 500 responses; error responses include structured detail field</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Grep confirms env validation wired: `grep -r "requireEnv" src/`
3. Grep confirms all API catch blocks log: `grep -rn "console.error" src/app/api/`
4. Grep confirms await on streamText: `grep "await streamText" src/app/api/hints/route.ts`
</verification>

<success_criteria>
- Missing XAI_API_KEY throws with clear message naming the variable
- All 4 API routes with catch blocks log errors with console.error before responding
- /api/hints awaits streamText so pre-stream errors return JSON, not broken SSE
- LLM null output error message identifies schema mismatch
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/14-api-rendering-stability/14-01-SUMMARY.md`
</output>
