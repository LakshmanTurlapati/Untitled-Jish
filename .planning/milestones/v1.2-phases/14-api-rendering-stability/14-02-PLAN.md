---
phase: 14-api-rendering-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/VocabularyList.tsx
  - src/app/components/QuizView.tsx
  - src/app/components/WordBreakdown.tsx
  - src/app/components/AnalysisView.tsx
  - src/app/components/KaavyaLibrary.tsx
autonomous: true
requirements: [STAB-01]

must_haves:
  truths:
    - "React lists in all 5 components use stable unique keys derived from content, not array indices"
    - "No console warnings about duplicate or unstable keys in dev mode"
  artifacts:
    - path: "src/app/components/VocabularyList.tsx"
      provides: "Stable keys using word stem+original"
      contains: "word.stem"
    - path: "src/app/components/QuizView.tsx"
      provides: "Stable keys for hearts and confetti"
      contains: "heart-"
    - path: "src/app/components/WordBreakdown.tsx"
      provides: "Stable keys for samasa components"
      contains: "comp.iast"
    - path: "src/app/components/AnalysisView.tsx"
      provides: "Stable keys for progress steps and word results"
      contains: "step-"
    - path: "src/app/components/KaavyaLibrary.tsx"
      provides: "Stable keys for skeleton placeholders"
      contains: "skeleton-"
  key_links: []
---

<objective>
Replace all React array index keys with stable unique keys across 5 components to eliminate console warnings and prevent rendering bugs during list reordering.

Purpose: Array index keys (key={i}, key={index}) cause React reconciliation issues when lists are reordered, filtered, or modified. Every list in the app should use a key derived from the item's content or a stable identifier.

Output: All 7 key sites across 5 components use stable, content-derived keys.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-api-rendering-stability/14-CONTEXT.md

@src/app/components/VocabularyList.tsx
@src/app/components/QuizView.tsx
@src/app/components/WordBreakdown.tsx
@src/app/components/AnalysisView.tsx
@src/app/components/KaavyaLibrary.tsx
</context>

<interfaces>
<!-- Key types for determining stable key values -->

VocabularyWord (from src/lib/study/types.ts):
```typescript
export interface VocabularyWord {
  original: string;  // Devanagari
  iast: string;      // IAST transliteration
  stem: string;      // normalized stem (lowercase)
  wordType: WordType;
  linga: Linga | undefined;
  contextualMeaning: string;
  mwDefinition: string | null;
}
```

EnrichedWord (extends AnalyzedWord, from src/lib/analysis/types.ts):
```typescript
// AnalyzedWord has: original, iast, sandhi, morphology, samasa, contextual_meaning, inria_validated, inria_grammar
export interface EnrichedWord extends AnalyzedWord {
  mw_definitions: string[];
  apte_definitions: string[];
  meaning_source: MeaningSource;
}
```

Samasa component shape (from FullAnalysisSchema):
```typescript
// samasa.components[]: { iast: string, meaning: string }
```

PROGRESS_STEPS constant in AnalysisView.tsx:
```typescript
const PROGRESS_STEPS = [
  "Splitting sandhi...",
  "Analyzing morphology...",
  "Looking up meanings...",
];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace array index keys with stable keys in all 5 components</name>
  <files>src/app/components/VocabularyList.tsx, src/app/components/QuizView.tsx, src/app/components/WordBreakdown.tsx, src/app/components/AnalysisView.tsx, src/app/components/KaavyaLibrary.tsx</files>
  <read_first>
    - src/app/components/VocabularyList.tsx (line 48-50: vocabulary.map key={index})
    - src/app/components/QuizView.tsx (line 49-51: hearts key={i}, line 76-83: confetti key={i})
    - src/app/components/WordBreakdown.tsx (line 82-83: samasa components key={i})
    - src/app/components/AnalysisView.tsx (line 121-125: PROGRESS_STEPS key={i}, line 163-164: WordBreakdown key={index})
    - src/app/components/KaavyaLibrary.tsx (line 71-73: skeleton key={i})
  </read_first>
  <action>
    Apply these 7 key replacements per the decisions in CONTEXT.md (per STAB-01):

    1. VocabularyList.tsx line 50: Change `key={index}` to `key={word.stem || word.original}`. The stem is lowercase-normalized and unique per extractVocabulary's dedup logic.

    2. QuizView.tsx Hearts component line 51: Change `key={i}` to `key={\`heart-${i}\`}`. Static fixed-length array of hearts -- index is acceptable content-wise but the string prefix makes React reconciliation intent explicit and eliminates the console warning.

    3. QuizView.tsx Confetti component line 83: Change `key={i}` to `key={\`confetti-${i}\`}`. Static decorative array generated from fixed length 25 -- same rationale as hearts.

    4. WordBreakdown.tsx line 83: Change `key={i}` to `key={\`${comp.iast}-${comp.meaning}\`}`. Samasa components have unique iast+meaning combinations within a single word.

    5. AnalysisView.tsx line 125 (progress steps): Change `key={i}` to `key={step}`. PROGRESS_STEPS is a static string array with unique values -- the step text itself is the stable key.

    6. AnalysisView.tsx line 164 (word results): Change `key={index}` to `key={\`${word.original}-${word.iast}-${index}\`}`. Include original+iast for content identity plus index as tiebreaker since the same word can appear multiple times in a passage.

    7. KaavyaLibrary.tsx line 73: Change `key={i}` to `key={\`skeleton-${i}\`}`. Static placeholder array [1,2,3,4] -- prefix makes intent explicit.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Untitled\ Jish && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - VocabularyList uses key={word.stem || word.original}
    - QuizView Hearts uses key={`heart-${i}`}
    - QuizView Confetti uses key={`confetti-${i}`}
    - WordBreakdown samasa uses key={`${comp.iast}-${comp.meaning}`}
    - AnalysisView steps uses key={step}
    - AnalysisView word results uses key={`${word.original}-${word.iast}-${index}`}
    - KaavyaLibrary skeletons uses key={`skeleton-${i}`}
    - No remaining key={i} or key={index} patterns in these 5 files
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All 7 key sites across 5 components use stable content-derived keys; no array index keys remain in any component</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. No index keys remain: `grep -rn "key={i}\|key={index}" src/app/components/` returns no matches
3. Stable keys confirmed: `grep -rn "key={" src/app/components/{VocabularyList,QuizView,WordBreakdown,AnalysisView,KaavyaLibrary}.tsx`
</verification>

<success_criteria>
- Zero instances of key={i} or key={index} across VocabularyList, QuizView, WordBreakdown, AnalysisView, KaavyaLibrary
- All keys use stable identifiers: content text, stem/original, or prefixed strings
- TypeScript compiles clean
- No console key warnings would appear in dev mode
</success_criteria>

<output>
After completion, create `.planning/phases/14-api-rendering-stability/14-02-SUMMARY.md`
</output>
