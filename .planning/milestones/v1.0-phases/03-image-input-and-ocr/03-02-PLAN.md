---
phase: 03-image-input-and-ocr
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/components/AnalysisView.tsx
  - src/app/components/ImageUpload.tsx
  - src/__tests__/text-input.test.tsx
  - src/__tests__/image-upload.test.tsx
  - vitest.config.ts
autonomous: false
requirements: [INPUT-01, INPUT-02]

must_haves:
  truths:
    - "User can type or paste Devanagari text and see live IAST transliteration below the input"
    - "User can upload a JPEG or PNG image and see extracted text populate the text input"
    - "User sees image preview after selecting a file"
    - "User sees loading state during OCR extraction"
    - "User sees error message if upload fails or file type is invalid"
    - "Extracted OCR text feeds into the existing analysis pipeline when user clicks Analyze"
  artifacts:
    - path: "src/app/components/AnalysisView.tsx"
      provides: "Enhanced text input with IAST preview and ImageUpload integration"
      contains: "devanagariToIast"
    - path: "src/app/components/ImageUpload.tsx"
      provides: "Drag-drop/click image upload component with preview and OCR"
      exports: ["ImageUpload"]
    - path: "src/__tests__/text-input.test.tsx"
      provides: "Tests for IAST transliteration preview display"
    - path: "src/__tests__/image-upload.test.tsx"
      provides: "Tests for image upload component behavior"
  key_links:
    - from: "src/app/components/ImageUpload.tsx"
      to: "/api/ocr"
      via: "fetch POST with FormData"
      pattern: "fetch.*api/ocr"
    - from: "src/app/components/AnalysisView.tsx"
      to: "src/app/components/ImageUpload.tsx"
      via: "ImageUpload onTextExtracted callback"
      pattern: "onTextExtracted"
    - from: "src/app/components/AnalysisView.tsx"
      to: "src/lib/transliteration.ts"
      via: "devanagariToIast for live preview"
      pattern: "devanagariToIast"
---

<objective>
Text input with IAST preview, ImageUpload component, and OCR-to-analysis pipeline wiring.

Purpose: Complete the user-facing input layer -- users can either type Devanagari text (with live IAST transliteration) or upload an image (OCR extracts text into the input). Both paths feed into the existing analysis pipeline.
Output: Enhanced AnalysisView with IAST preview, new ImageUpload component, component tests.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-image-input-and-ocr/03-01-SUMMARY.md
@src/app/components/AnalysisView.tsx
@src/lib/transliteration.ts
@src/app/page.tsx
@vitest.config.ts

<interfaces>
<!-- From Plan 01 - OCR API endpoint contract -->
POST /api/ocr
  Request: FormData with "image" field (File, JPEG/PNG, max 20MB)
  Response 200: { text: string }
  Response 400: { error: string }
  Response 500: { error: string }

<!-- Existing transliteration utility -->
From src/lib/transliteration.ts:
```typescript
export function devanagariToIast(text: string): string;
```

<!-- Existing AnalysisView component to modify -->
From src/app/components/AnalysisView.tsx:
```typescript
// Current: textarea + submit button + results grid
// Has: inputText state, handleSubmit, isLoading, error, analysisResult
// Needs: IAST preview below textarea, ImageUpload component integration
```

<!-- Existing design tokens (from Phase 1) -->
Tailwind tokens: parchment-50/100/200, ink-600/700/800/900, accent-500/600
Font class: font-sanskrit (Shobhika)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IAST preview in AnalysisView and ImageUpload component</name>
  <files>src/app/components/AnalysisView.tsx, src/app/components/ImageUpload.tsx, src/__tests__/text-input.test.tsx, src/__tests__/image-upload.test.tsx, vitest.config.ts</files>
  <behavior>
    Text input tests (src/__tests__/text-input.test.tsx):
    - Test 1: IAST transliteration preview is displayed when Devanagari text is entered
    - Test 2: IAST preview is empty/hidden when textarea is empty
    - Test 3: Textarea accepts Devanagari input and updates state

    Image upload tests (src/__tests__/image-upload.test.tsx):
    - Test 1: Renders file input accepting JPEG and PNG
    - Test 2: Shows error for unsupported file types
    - Test 3: Calls onTextExtracted with OCR result on successful upload
    - Test 4: Shows loading state during upload
    - Test 5: Shows error message on upload failure
  </behavior>
  <action>
    **Update vitest.config.ts:**
    Add the new .tsx test files to environmentMatchGlobs for jsdom:
    ```
    environmentMatchGlobs: [
      ["src/__tests__/word-breakdown.test.tsx", "jsdom"],
      ["src/__tests__/text-input.test.tsx", "jsdom"],
      ["src/__tests__/image-upload.test.tsx", "jsdom"],
    ],
    ```

    **Create `src/app/components/ImageUpload.tsx`:**
    - Props: `{ onTextExtracted: (text: string) => void }`
    - State: isUploading (boolean), preview (string | null for object URL), error (string | null)
    - `handleFile(file: File)` callback:
      - Validate file.type is "image/jpeg" or "image/png" (set error if not)
      - Validate file.size <= 20MB (set error if not)
      - Set preview via `URL.createObjectURL(file)`
      - Set isUploading=true, clear error
      - POST FormData with "image" field to "/api/ocr"
      - On success: call `onTextExtracted(data.text)`
      - On failure: set error message
      - Finally: set isUploading=false
    - Render:
      - A drop zone div with dashed border (border-dashed border-parchment-200) and click-to-upload text
      - Hidden file input (accept="image/jpeg,image/png") triggered by div click
      - Drag-and-drop: onDragOver preventDefault, onDrop extract files[0] and call handleFile
      - Image preview (if preview set): `<img src={preview} alt="Upload preview" className="max-h-48 rounded" />`
      - Loading indicator when isUploading: "Extracting text..." with spinner or pulse
      - Error display (if error set): red text

    **Modify `src/app/components/AnalysisView.tsx`:**
    - Import `devanagariToIast` from `@/lib/transliteration`
    - Import `ImageUpload` from `./ImageUpload`
    - Compute `iastPreview = inputText ? devanagariToIast(inputText) : ""`
    - Add IAST preview below the textarea:
      ```tsx
      {iastPreview && (
        <p className="mt-2 rounded bg-parchment-50 px-3 py-2 text-sm italic text-ink-600">
          {iastPreview}
        </p>
      )}
      ```
    - Add ImageUpload component above or below the form with a section divider:
      ```tsx
      <div className="mb-6">
        <p className="mb-2 text-xs font-semibold uppercase tracking-widest text-accent-600">
          Upload Image
        </p>
        <ImageUpload onTextExtracted={(text) => setInputText(text)} />
      </div>
      ```
    - When onTextExtracted fires, the extracted text populates the textarea via setInputText, so user can review before clicking Analyze

    **Create tests:**
    - `src/__tests__/text-input.test.tsx`: Import AnalysisView, render it, type Devanagari text into textarea via fireEvent.change, assert IAST preview text appears. Mock fetch to prevent actual API calls.
    - `src/__tests__/image-upload.test.tsx`: Import ImageUpload, render with mock onTextExtracted callback. Mock global fetch. Test file input change event with valid/invalid files. Test loading and error states. Use `@vitest-environment jsdom` directive at top of each file.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npx vitest run src/__tests__/text-input.test.tsx src/__tests__/image-upload.test.tsx -x</automated>
  </verify>
  <done>AnalysisView shows live IAST preview below textarea. ImageUpload component handles file selection, validation, upload, preview, loading, and error states. OCR-extracted text populates the textarea for user review before analysis. All component tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete input flow (text + image + OCR + analysis)</name>
  <files>n/a</files>
  <action>
    Human verifies the complete input layer works end-to-end:

    1. Run the dev server: `npm run dev`
    2. Visit http://localhost:3000
    3. TEXT INPUT TEST:
       - Type or paste Devanagari text (e.g. "dharmaksetre kuruksetre") into the textarea
       - Verify IAST transliteration appears below the textarea
       - Click Analyze and verify the full analysis pipeline still works
    4. IMAGE UPLOAD TEST:
       - Find or photograph a page of printed Sanskrit Devanagari text
       - Upload the image via the upload area
       - Verify: image preview appears, loading state shows, extracted text populates the textarea
       - Review the extracted text for accuracy
       - Click Analyze to run the full pipeline on the extracted text
    5. ERROR HANDLING TEST:
       - Try uploading a non-image file (e.g. .txt) -- should show error
       - Verify the UI looks consistent with the existing academic design
  </action>
  <verify>Human confirms text input with IAST preview, image upload with OCR, and end-to-end analysis pipeline all work correctly</verify>
  <done>User approves the complete input flow: text entry with IAST, image upload with OCR extraction, and analysis results</done>
</task>

</tasks>

<verification>
All tests pass: `npx vitest run --reporter=verbose`
IAST preview updates live as user types Devanagari.
Image upload sends to /api/ocr and populates textarea with extracted text.
Full pipeline works: image -> OCR -> text in textarea -> Analyze -> word breakdown.
</verification>

<success_criteria>
- Typing Devanagari text shows live IAST transliteration preview below the textarea
- ImageUpload component renders with drag-drop/click interface
- Uploading a JPEG/PNG image extracts text via /api/ocr and populates the textarea
- User can review/edit extracted text before clicking Analyze
- Analysis pipeline produces the same results whether text was typed or OCR-extracted
- All new component tests pass
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/03-image-input-and-ocr/03-02-SUMMARY.md`
</output>
