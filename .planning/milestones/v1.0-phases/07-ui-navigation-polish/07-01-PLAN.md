---
phase: 07-ui-navigation-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/AnalysisView.tsx
  - src/__tests__/analysis-view-nav.test.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "User sees top-level Analyze/Study tabs that persist across interactions"
    - "Study tab is disabled when no analysis results exist"
    - "Sticky Analyze button only appears on the Analyze tab"
    - "Progress step checkmarks use accent color, not green"
    - "Switching tabs preserves inputText and analysisResult state"
  artifacts:
    - path: "src/app/components/AnalysisView.tsx"
      provides: "Top-level tab navigation, conditional sticky bar, accent checkmarks"
      contains: "pageTab"
    - path: "src/__tests__/analysis-view-nav.test.tsx"
      provides: "Tests for tab navigation, sticky bar gating, checkmark colors"
      min_lines: 40
  key_links:
    - from: "src/app/components/AnalysisView.tsx"
      to: "pageTab state"
      via: "useState driving conditional render of Analyze vs Study content"
      pattern: "pageTab.*analyze|pageTab.*study"
    - from: "src/app/components/AnalysisView.tsx"
      to: "sticky bar"
      via: "pageTab === analyze gates sticky bar render"
      pattern: "pageTab.*===.*analyze.*fixed bottom"
---

<objective>
Restructure AnalysisView with top-level Analyze/Study tab navigation, gate the sticky Analyze button to the Analyze tab only, and fix progress step checkmark colors from green to accent.

Purpose: Clean up navigation so input/analysis and study/quiz are distinct sections, remove visual clutter of the Analyze button during quiz, and align checkmark colors with the warm academic palette.
Output: Updated AnalysisView.tsx with page-level tabs and gated sticky bar, plus new test file.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-navigation-polish/07-RESEARCH.md

@src/app/components/AnalysisView.tsx
@src/app/page.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/analysis/types.ts:
```typescript
export interface EnrichedWord {
  // word analysis properties used by WordBreakdown, VocabularyList, QuizView
}
```

From src/app/components/AnalysisView.tsx (current structure):
- State: inputText, analysisResult (EnrichedWord[] | null), isLoading, error, activeTab (words|vocabulary|quiz), completedSteps
- The existing 3-tab navigation (words/vocabulary/quiz) appears only after analysisResult exists
- Sticky bottom bar is always rendered (currently unconditional)
- Progress step checkmarks use `bg-green-500` (line 124)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests for tab navigation, sticky bar gating, and checkmark colors</name>
  <files>src/__tests__/analysis-view-nav.test.tsx</files>
  <behavior>
    - Test: Analyze tab is active by default, Study tab visible but disabled when no results
    - Test: After analysis results exist, Study tab becomes enabled
    - Test: Clicking Study tab shows vocabulary/quiz content, hides input card
    - Test: Sticky bottom bar with "Analyze" button renders when on Analyze tab
    - Test: Sticky bottom bar does NOT render when on Study tab
    - Test: Progress step checkmarks use accent color class (bg-accent-600), NOT bg-green-500
  </behavior>
  <action>
Create `src/__tests__/analysis-view-nav.test.tsx` with `// @vitest-environment jsdom` directive. Mock fetch globally. Tests should:

1. Render AnalysisView and verify two top-level tab buttons exist: "Analyze" and "Study"
2. Verify "Study" button has `disabled` attribute when analysisResult is null
3. Mock a successful analysis response, trigger submit, then verify "Study" tab becomes clickable
4. After clicking "Study" tab, verify vocabulary/quiz sub-content area appears and input card is hidden
5. Verify sticky bar (containing "Analyze" button in a fixed-bottom container) is present when on Analyze tab
6. After switching to Study tab, verify the fixed-bottom sticky bar is NOT in the document
7. Trigger loading state and verify completed step checkmarks have class `bg-accent-600` (not `bg-green-500`)

Use `fireEvent` for interactions (matches existing project test patterns). Mock fetch to return `{ words: [{ original: "dharma", stem: "dharma", meanings: { mw_definitions: ["virtue"], apte_definitions: [], contextual_meaning: "righteousness", meaning_source: "both" }, morphology: { case: "nominative", number: "singular", gender: "masculine" } }] }` for analysis result.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npx vitest run src/__tests__/analysis-view-nav.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All test cases fail (RED phase) because AnalysisView.tsx does not yet have pageTab state or conditional sticky bar</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AnalysisView with top-level tabs, gated sticky bar, and accent checkmarks</name>
  <files>src/app/components/AnalysisView.tsx</files>
  <action>
Modify `src/app/components/AnalysisView.tsx`:

1. **Add page-level tab state** above existing `activeTab`:
   ```
   const [pageTab, setPageTab] = useState<"analyze" | "study">("analyze");
   ```

2. **Add top-level tab navigation** at the very top of the return JSX (before the Hero Input Card). Two pill buttons: "Analyze" and "Study". Study is disabled when `!analysisResult`. Active tab gets `bg-accent-600 text-white`, inactive gets `text-ink-700 hover:bg-parchment-200`. Disabled gets `disabled:opacity-40 disabled:cursor-not-allowed`.

3. **Wrap existing content in conditional renders:**
   - `pageTab === "analyze"` wraps: Hero Input Card, error state, progress steps, and the word results (when activeTab is "words"). The existing 3-tab bar (words/vocabulary/quiz) should be removed from here.
   - `pageTab === "study"` wraps: vocabulary and quiz views with sub-tab navigation. Show a "Study" sub-tab bar with "Vocabulary" and "Quiz" buttons. Add a local state or reuse activeTab for this. If no analysisResult, show nothing (button is disabled anyway).
   - When user is on Analyze tab and has results, show word breakdown results directly (no tab switching needed for words).

4. **Gate the sticky bottom bar:** Wrap the `fixed bottom-0` div with `{pageTab === "analyze" && ( ... )}` so it only renders on Analyze tab.

5. **Fix checkmark colors:** On line 124, change `bg-green-500` to `bg-accent-600` in the progress step checkmark span.

6. **Preserve state across tab switches:** `inputText`, `analysisResult`, `error`, `completedSteps` all live in the parent component and are not affected by pageTab changes. No changes needed for state preservation -- just ensure conditional rendering, not unmounting stateful children.

7. **Reset pageTab to "analyze" in handleSubmit** (alongside existing `setActiveTab("words")`) so new analysis always returns to Analyze view.

Do NOT change: handleSubmit logic, fetch calls, ImageUpload integration, WordBreakdown rendering, progress step logic (just color). Keep all existing imports.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass including new nav tests. Analyze/Study tabs work, sticky bar only on Analyze tab, checkmarks use accent color. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` -- full suite green (no regressions)
2. `grep -n "bg-green-500" src/app/components/AnalysisView.tsx` -- returns nothing (checkmark color fixed)
3. `grep -n "pageTab" src/app/components/AnalysisView.tsx` -- confirms tab state exists
4. `grep -n "fixed bottom" src/app/components/AnalysisView.tsx` -- appears inside pageTab conditional
</verification>

<success_criteria>
- AnalysisView has top-level Analyze/Study tabs always visible
- Study tab disabled without results, enabled with results
- Sticky Analyze button only renders on Analyze tab
- Progress checkmarks use bg-accent-600
- All 106+ existing tests still pass
- New navigation tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-navigation-polish/07-01-SUMMARY.md`
</output>
