---
phase: 04-study-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/study/types.ts
  - src/lib/study/particles.ts
  - src/lib/study/vocabulary.ts
  - src/lib/study/quiz.ts
  - src/app/api/distractors/route.ts
  - src/lib/__tests__/vocabulary.test.ts
  - src/lib/__tests__/quiz.test.ts
autonomous: true
requirements: [STDY-01, STDY-02]

must_haves:
  truths:
    - "Vocabulary extraction filters common particles and deduplicates by stem"
    - "Vocabulary preserves word appearance order from analyzed text"
    - "Quiz generates 4-option MCQ with contextual_meaning as correct answer"
    - "Quiz draws distractors from sibling word meanings"
    - "Fallback distractors available from MW dictionary when text has < 4 unique words"
  artifacts:
    - path: "src/lib/study/types.ts"
      provides: "VocabularyWord, QuizQuestion, QuizPhase, QuizState type contracts"
      exports: ["VocabularyWord", "QuizQuestion", "QuizPhase", "QuizState"]
    - path: "src/lib/study/particles.ts"
      provides: "COMMON_PARTICLES set for filtering"
      exports: ["COMMON_PARTICLES"]
    - path: "src/lib/study/vocabulary.ts"
      provides: "extractVocabulary function"
      exports: ["extractVocabulary"]
    - path: "src/lib/study/quiz.ts"
      provides: "generateQuiz function"
      exports: ["generateQuiz"]
    - path: "src/app/api/distractors/route.ts"
      provides: "GET endpoint for MW fallback distractors"
      exports: ["GET"]
  key_links:
    - from: "src/lib/study/vocabulary.ts"
      to: "src/lib/analysis/types.ts"
      via: "imports EnrichedWord type"
      pattern: "import.*EnrichedWord.*from.*analysis/types"
    - from: "src/lib/study/quiz.ts"
      to: "src/lib/study/types.ts"
      via: "imports VocabularyWord, QuizQuestion"
      pattern: "import.*VocabularyWord.*QuizQuestion.*from.*types"
    - from: "src/app/api/distractors/route.ts"
      to: "src/lib/dictionary/db.ts"
      via: "imports getDb for SQLite queries"
      pattern: "import.*getDb.*from.*dictionary/db"
---

<objective>
Study feature logic: type contracts, vocabulary extraction, quiz generation, and fallback distractor API.

Purpose: Establish all data layer and logic for study features before building UI. Vocabulary extraction filters EnrichedWord[] into a deduplicated word list. Quiz generation creates MCQ questions from that list. Fallback API provides MW dictionary distractors for short texts.

Output: 5 source files (types, particles, vocabulary, quiz, API route) + 2 test files
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-study-features/04-CONTEXT.md
@.planning/phases/04-study-features/04-RESEARCH.md

<interfaces>
<!-- Key types from existing codebase that this plan depends on -->

From src/lib/analysis/types.ts:
```typescript
export type WordType = "noun" | "verb" | "adjective" | "indeclinable" | "participle" | "pronoun";
export type Linga = "pullinga" | "strilinga" | "napumsakalinga";

export interface MorphologyInfo {
  stem: string;
  word_type: WordType;
  vibhakti?: string;
  vacana?: Vacana;
  linga?: Linga;
  dhatu?: string;
  gana?: number;
  lakara?: string;
  purusha?: string;
}

export interface EnrichedWord extends AnalyzedWord {
  mw_definitions: string[];
  apte_definitions: string[];
  meaning_source: MeaningSource;
}
// AnalyzedWord has: original, iast, sandhi, morphology, samasa, contextual_meaning, inria_validated, inria_grammar
```

From src/lib/dictionary/db.ts:
```typescript
export function getDb(): Database.Database;
// Readonly SQLite connection to data/sanskrit.db, WAL mode, 64MB cache
// Tables: entries (headword, dictionary, definition), stems (inflection, stem, grammar)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type contracts, particles, and vocabulary extraction with tests</name>
  <files>src/lib/study/types.ts, src/lib/study/particles.ts, src/lib/study/vocabulary.ts, src/lib/__tests__/vocabulary.test.ts</files>
  <behavior>
    - extractVocabulary([word with iast="ca"]) returns [] (particle filtered)
    - extractVocabulary([word1 stem="dharma", word2 stem="dharma"]) returns 1 entry (deduplicated by stem)
    - extractVocabulary([word1, word2, word3]) preserves appearance order (first occurrence kept)
    - extractVocabulary returns VocabularyWord[] with original, iast, stem, wordType, linga, contextualMeaning, mwDefinition
    - extractVocabulary on empty array returns []
    - COMMON_PARTICLES contains at least: ca, tu, hi, eva, api, iti, atha, tatha, yatha, na, va
  </behavior>
  <action>
    1. Create src/lib/study/types.ts with these exports:
       - VocabularyWord: { original: string, iast: string, stem: string, wordType: WordType, linga: Linga | undefined, contextualMeaning: string, mwDefinition: string | null }
       - QuizQuestion: { word: { original: string, iast: string }, correctAnswer: string, options: string[] }
       - QuizPhase: "ready" | "active" | "answered" | "complete"
       - QuizState: { questions: QuizQuestion[], currentIndex: number, phase: QuizPhase, selectedAnswer: string | null, score: number }
       Import WordType and Linga from @/lib/analysis/types.

    2. Create src/lib/study/particles.ts with COMMON_PARTICLES as a Set of lowercase IAST strings:
       ca, tu, hi, eva, api, iti, atha, tatha, yatha, na, va, iva, aho, kim, tat, yat, sma, ha, vai, khalu

    3. Create src/lib/study/vocabulary.ts with extractVocabulary(words: EnrichedWord[]): VocabularyWord[]:
       - Iterate words in order, filter by checking COMMON_PARTICLES.has(word.iast.toLowerCase())
       - Deduplicate by word.morphology.stem.toLowerCase() using a Set
       - Map to VocabularyWord: { original: word.original, iast: word.iast, stem: word.morphology.stem, wordType: word.morphology.word_type, linga: word.morphology.linga, contextualMeaning: word.contextual_meaning, mwDefinition: word.mw_definitions[0] ?? null }
       - Return array preserving first-occurrence order

    4. Create src/lib/__tests__/vocabulary.test.ts:
       - Write tests FIRST per TDD (RED phase), then implement (GREEN phase)
       - Use a helper function to create mock EnrichedWord objects for test data
       - Test: filters particles (ca, tu, hi, eva, api)
       - Test: deduplicates by stem (two words with same stem -> one entry)
       - Test: preserves appearance order
       - Test: maps all VocabularyWord fields correctly
       - Test: empty input returns empty array
       - Test: COMMON_PARTICLES set has expected members
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npx vitest run src/lib/__tests__/vocabulary.test.ts --reporter=verbose</automated>
  </verify>
  <done>extractVocabulary correctly filters particles, deduplicates by stem, preserves order, and maps to VocabularyWord type. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Quiz generation logic, fallback distractor API, and tests</name>
  <files>src/lib/study/quiz.ts, src/app/api/distractors/route.ts, src/lib/__tests__/quiz.test.ts</files>
  <behavior>
    - generateQuiz with 5+ vocabulary words produces questions with 4 options each
    - Each question's correctAnswer equals the word's contextualMeaning
    - Distractors come from other words' contextualMeaning values
    - generateQuiz with fallbackMeanings uses them when vocabulary has < 4 words
    - Questions capped at 10 maximum per user discretion
    - generateQuiz with fewer than 4 vocabulary words and no fallback returns empty array
    - GET /api/distractors?count=3 returns { meanings: string[] } with 3 items
  </behavior>
  <action>
    1. Create src/lib/study/quiz.ts with generateQuiz(vocabulary: VocabularyWord[], fallbackMeanings?: string[]): QuizQuestion[]:
       - If vocabulary.length < 4 and no fallbackMeanings or fallbackMeanings.length + vocabulary.length < 4, return [] (need at least 4 total meanings for 1 correct + 3 distractors)
       - Shuffle vocabulary, cap at 10 questions
       - For each word: correctAnswer = word.contextualMeaning
       - Draw 3 distractors from other words' contextualMeaning values
       - If not enough sibling meanings (< 3), pad with fallbackMeanings
       - Shuffle the 4 options array
       - Return QuizQuestion[] with word: { original, iast }, correctAnswer, options
       - Export a pickRandom helper (for testability): picks n unique random items from array

    2. Create src/app/api/distractors/route.ts:
       - GET handler, reads "count" query param (default 3, max 10)
       - Queries getDb(): SELECT definition FROM entries WHERE dictionary = 'mw' AND definition != '' ORDER BY RANDOM() LIMIT ?
       - Truncates definitions: take substring before first comma if comma exists within 80 chars, else take first 80 chars
       - Returns NextResponse.json({ meanings: string[] })

    3. Create src/lib/__tests__/quiz.test.ts:
       - Write tests FIRST per TDD, then implement
       - Create mock VocabularyWord fixtures (5-6 words with distinct contextualMeaning values)
       - Test: generates questions with 4 options each
       - Test: correctAnswer matches word.contextualMeaning
       - Test: distractors come from sibling words (verify each distractor exists in another word's contextualMeaning)
       - Test: questions capped at 10 (provide 15 vocabulary items)
       - Test: returns empty when < 4 words and no fallback
       - Test: uses fallbackMeanings to pad distractors when vocabulary is small
       - Test: each question's options array contains the correctAnswer
       - Note: do NOT test the API route here -- that will be verified by integration
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npx vitest run src/lib/__tests__/quiz.test.ts --reporter=verbose</automated>
  </verify>
  <done>generateQuiz produces valid MCQ questions with correct answers and distractors. Questions capped at 10. Fallback distractors work for small vocabularies. Distractor API route created. All tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```bash
npx vitest run --reporter=verbose
```

Verify all new files exist:
```bash
ls src/lib/study/types.ts src/lib/study/particles.ts src/lib/study/vocabulary.ts src/lib/study/quiz.ts src/app/api/distractors/route.ts
```
</verification>

<success_criteria>
- extractVocabulary filters particles, deduplicates by stem, preserves order
- generateQuiz creates 4-option MCQ with contextual_meaning as correct answer
- Distractor API returns random MW definitions
- All vocabulary and quiz tests pass
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-study-features/04-01-SUMMARY.md`
</output>
