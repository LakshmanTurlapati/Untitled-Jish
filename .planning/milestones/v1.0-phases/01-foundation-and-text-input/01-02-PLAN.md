---
phase: 01-foundation-and-text-input
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/import-dictionary.ts
  - scripts/build-stem-index.ts
  - scripts/setup-data.sh
  - scripts/__tests__/import-dictionary.test.ts
  - src/lib/dictionary/db.ts
  - src/lib/dictionary/lookup.ts
  - src/lib/dictionary/schema.ts
  - src/lib/__tests__/dictionary.test.ts
  - src/lib/__tests__/stem-index.test.ts
  - src/app/api/dictionary/route.ts
  - data/.gitkeep
autonomous: true
requirements: [ANAL-05, UI-01]

must_haves:
  truths:
    - "Dictionary lookup returns Monier-Williams entries for Sanskrit stem forms"
    - "Dictionary lookup returns Apte entries as secondary source"
    - "Inflected forms resolve to their stem forms via the reverse index"
    - "Dictionary entries include full content: definition, grammar, etymology"
    - "Dictionary API endpoint returns JSON results for headword queries"
  artifacts:
    - path: "scripts/import-dictionary.ts"
      provides: "CDSL parser and SQLite importer for MW and AP90"
      min_lines: 100
    - path: "scripts/build-stem-index.ts"
      provides: "INRIA morphological data importer for inflection-to-stem index"
      min_lines: 50
    - path: "src/lib/dictionary/db.ts"
      provides: "SQLite connection singleton (server-only)"
      exports: ["getDb"]
    - path: "src/lib/dictionary/lookup.ts"
      provides: "Dictionary query functions"
      exports: ["lookupByHeadword", "lookupByStem", "searchEntries"]
    - path: "src/lib/dictionary/schema.ts"
      provides: "TypeScript types for dictionary entries"
      exports: ["DictionaryEntry", "StemIndexEntry", "DictionaryName"]
    - path: "src/app/api/dictionary/route.ts"
      provides: "GET endpoint for dictionary lookups"
      exports: ["GET"]
    - path: "data/sanskrit.db"
      provides: "Populated SQLite database with MW + AP90 entries and stem index"
  key_links:
    - from: "src/app/api/dictionary/route.ts"
      to: "src/lib/dictionary/lookup.ts"
      via: "import lookup functions"
      pattern: "import.*lookup"
    - from: "src/lib/dictionary/lookup.ts"
      to: "src/lib/dictionary/db.ts"
      via: "getDb() call"
      pattern: "getDb\\(\\)"
    - from: "scripts/import-dictionary.ts"
      to: "src/lib/transliteration.ts"
      via: "SLP1 conversion during import"
      pattern: "slp1To(Iast|Devanagari)"
---

<objective>
Build the complete dictionary infrastructure: CDSL parser for Monier-Williams and Apte dictionaries, SQLite import pipeline, inflection-to-stem reverse index, dictionary lookup module, and API endpoint.

Purpose: Dictionary data is the foundation for all analysis in later phases. Every word lookup, meaning display, and morphological cross-reference depends on this infrastructure. The stem index bridges the gap between inflected text forms and dictionary headwords.
Output: Populated SQLite database with 280K+ MW entries and AP90 entries, working lookup functions, stem index, and API route.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-text-input/01-CONTEXT.md
@.planning/phases/01-foundation-and-text-input/01-RESEARCH.md
@.planning/phases/01-foundation-and-text-input/01-VALIDATION.md
@.planning/phases/01-foundation-and-text-input/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - transliteration utility needed for SLP1 conversion during import -->
From src/lib/transliteration.ts:
```typescript
export function devanagariToIast(text: string): string;
export function iastToDevanagari(text: string): string;
export function slp1ToDevanagari(text: string): string;
export function slp1ToIast(text: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CDSL parser and SQLite dictionary import pipeline</name>
  <files>
    src/lib/dictionary/schema.ts, scripts/import-dictionary.ts,
    scripts/__tests__/import-dictionary.test.ts, scripts/setup-data.sh,
    data/.gitkeep
  </files>
  <behavior>
    - Parser extracts headword (key1) from H1/H1A/H2/H3/H4 tagged lines
    - Parser extracts definition body from body tags
    - Parser extracts grammatical info from lex tags within body
    - Parser extracts L-number and page reference from tail section
    - Parser handles lines with missing optional fields (etymology, homonym) gracefully
    - SLP1 headwords are converted to both Devanagari and IAST during parsing
    - Import creates SQLite database with entries table and FTS5 index
    - MW and AP90 entries are distinguished by dictionary column
  </behavior>
  <action>
    1. Create `src/lib/dictionary/schema.ts`:
       - Define TypeScript types: DictionaryEntry (id, dictionary, headword_slp1, headword_deva, headword_iast, homonym, grammar, definition, etymology, raw_body, page_ref, l_number), StemIndexEntry (inflected forms in 3 scripts, stem in 3 scripts, grammar_info), DictionaryName ('mw' | 'ap90')
       - These types mirror the SQLite schema from research

    2. Create `scripts/__tests__/import-dictionary.test.ts` FIRST (TDD red):
       - Test parseEntryLine with a real MW entry line (use example from research):
         ```
         <H1><h><key1>aBi</key1><key2>aBi</key2></h><body><lex>ind.</lex> a prefix to verbs and nouns</body><tail><L>60</L><pc>1,1</pc></tail></H1>
         ```
       - Verify key1='aBi', body contains definition, lex=['ind.'], L='60', pc='1,1'
       - Test with H2, H3 entry types
       - Test with entry containing homonym number
       - Test with entry missing optional fields
       - Test SLP1-to-IAST conversion of headword (aBi -> abhi)
       - Test that createDatabase creates the expected tables (entries, entries_fts, stem_index)
       - Run tests: MUST fail

    3. Create `scripts/setup-data.sh`:
       - Downloads MW dictionary data from CDSL GitHub: `https://raw.githubusercontent.com/sanskrit-lexicon/csl-orig/master/v02/mw/mw.txt`
       - Downloads AP90 dictionary data: `https://raw.githubusercontent.com/sanskrit-lexicon/csl-orig/master/v02/ap90/ap90.txt`
       - Downloads INRIA morphological data: `https://sanskrit.inria.fr/DATA/XML/SL_morph.xml.gz`
       - Creates data/cdsl/ directory, downloads into it
       - Gunzips the INRIA file
       - Make executable with `chmod +x`

    4. Create `scripts/import-dictionary.ts` (TDD green):
       - Run with: `npx tsx scripts/import-dictionary.ts`
       - Import better-sqlite3 and transliteration utilities
       - Function `createDatabase(dbPath)`: Creates SQLite database with:
         - `entries` table matching the schema from research (id, dictionary, headword_slp1, headword_deva, headword_iast, homonym, grammar, definition, etymology, raw_body, page_ref, l_number)
         - Indexes on headword_iast, headword_deva, dictionary
         - FTS5 virtual table `entries_fts` on headword_iast, headword_deva, definition
         - `stem_index` table (inflected_form_slp1, inflected_form_iast, inflected_form_deva, stem_slp1, stem_iast, stem_deva, grammar_info) with composite primary key
         - Indexes on stem_index inflected form columns
       - Function `parseEntryLine(line)`: Parses a single CDSL entry line:
         - Match H-type tag at start (H1, H1A, H1B, H2, H2A, H3, H3A, H4, etc.)
         - Extract key1, key2, homonym from `<h>` section
         - Extract body content from `<body>` section
         - Extract lex tags from within body
         - Extract L-number and pc from `<tail>` section
         - Convert SLP1 headword to Devanagari and IAST using transliteration module
         - Clean definition text: strip internal markup tags, preserve readable text
         - Return null for non-entry lines
       - Function `importDictionary(db, filePath, dictName)`:
         - Read file line by line
         - Parse each line with parseEntryLine
         - Batch insert using prepared statement (use transaction for performance)
         - Log progress every 50,000 entries
       - Main execution:
         - Create database at `data/sanskrit.db`
         - Import MW from `data/cdsl/mw.txt`
         - Import AP90 from `data/cdsl/ap90.txt`
         - Rebuild FTS5 index after all imports
         - Log final entry counts per dictionary
       - Add script to package.json: `"import-dict": "tsx scripts/import-dictionary.ts"`
       - Run tests: MUST pass

    5. Create `data/.gitkeep` and add to .gitignore: `data/cdsl/`, `data/sanskrit.db` (raw downloads and built DB are gitignored, built by scripts)

    6. Run the import pipeline:
       - `bash scripts/setup-data.sh` to download dictionary data
       - `npm run import-dict` to parse and import into SQLite
       - Verify entry counts: MW should have ~280K entries, AP90 should have entries
       - If the CDSL format differs from expectations (e.g., AP90 uses different markup), adapt the parser. The MW format is well-documented; AP90 may have minor variations.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npx vitest run scripts/__tests__/import-dictionary.test.ts --reporter=verbose</automated>
  </verify>
  <done>CDSL parser correctly extracts entries from MW format. SQLite database created with entries table, FTS5 index, and stem_index table. MW and AP90 dictionaries imported with correct entry counts. Parser tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dictionary lookup module, stem index, and API route</name>
  <files>
    src/lib/dictionary/db.ts, src/lib/dictionary/lookup.ts,
    src/lib/__tests__/dictionary.test.ts, src/lib/__tests__/stem-index.test.ts,
    scripts/build-stem-index.ts, src/app/api/dictionary/route.ts
  </files>
  <behavior>
    - lookupByHeadword("dharma", "iast") returns MW and AP90 entries for dharma
    - lookupByHeadword returns entries with full content (definition, grammar, etymology)
    - lookupByStem resolves inflected forms to stems, then looks up stem entries
    - searchEntries performs FTS5 full-text search across headwords and definitions
    - GET /api/dictionary?q=dharma returns JSON array of dictionary entries
    - GET /api/dictionary?q=unknown returns empty array (not error)
    - Stem index resolves common inflected forms (e.g., nominal declensions) to stems
  </behavior>
  <action>
    1. Create `src/lib/dictionary/db.ts`:
       - Server-only SQLite connection singleton
       - Function `getDb()`: Creates/returns cached Database instance
       - Opens `data/sanskrit.db` in readonly mode
       - Sets pragmas: `journal_mode = WAL`, `cache_size = -64000` (64MB)
       - Path: `path.join(process.cwd(), 'data', 'sanskrit.db')`

    2. Create `src/lib/__tests__/dictionary.test.ts` FIRST (TDD red):
       - Test lookupByHeadword("dharma", "iast") returns results with dictionary='mw'
       - Test lookupByHeadword returns entries with non-empty definition
       - Test lookupByHeadword with Devanagari input ("धर्म", "deva")
       - Test lookupByHeadword for AP90 entries
       - Test searchEntries("dharma") returns results via FTS5
       - Test lookupByHeadword("nonexistentword12345", "iast") returns empty array
       - Note: These tests require the populated database from Task 1. If database doesn't exist, tests should skip gracefully with a clear message.
       - Run tests: MUST fail (lookup module doesn't exist)

    3. Create `src/lib/__tests__/stem-index.test.ts` FIRST (TDD red):
       - Test lookupByStem with a known inflected form resolves to correct stem
       - Test lookupByStem returns the grammar_info (e.g., "nom.sg.m")
       - Test lookupByStem with unknown inflected form returns empty array
       - Note: Requires populated stem_index table. Skip gracefully if DB missing.
       - Run tests: MUST fail

    4. Create `src/lib/dictionary/lookup.ts` (TDD green):
       - Import getDb from db.ts, types from schema.ts
       - Function `lookupByHeadword(headword: string, script: 'iast' | 'deva' | 'slp1')`:
         - Query entries table by the appropriate headword column based on script
         - Return DictionaryEntry[] ordered by dictionary (mw first, then ap90)
       - Function `lookupByStem(inflectedForm: string, script: 'iast' | 'deva' | 'slp1')`:
         - Query stem_index for matching inflected form
         - For each resolved stem, call lookupByHeadword
         - Return { stems: StemIndexEntry[], entries: DictionaryEntry[] }
       - Function `searchEntries(query: string, limit?: number)`:
         - Use FTS5 MATCH query on entries_fts
         - Join back to entries table for full data
         - Return DictionaryEntry[] with limit (default 20)
       - All functions use prepared statements for performance
       - Run tests: MUST pass

    5. Create `scripts/build-stem-index.ts`:
       - Run with: `npx tsx scripts/build-stem-index.ts`
       - Parse INRIA SL_morph.xml (gunzipped from setup-data.sh)
       - The INRIA XML contains inflected forms mapped to stems with grammatical info
       - For each entry: extract inflected form (SLP1), stem (SLP1), grammar info
       - Convert both to IAST and Devanagari using transliteration module
       - Batch insert into stem_index table (use transactions, batch of 10000)
       - Log progress and final count
       - Add to package.json: `"build-stems": "tsx scripts/build-stem-index.ts"`
       - Run the script: `npm run build-stems`
       - Run stem-index tests: MUST pass

    6. Create `src/app/api/dictionary/route.ts`:
       - Export async GET handler
       - Parse query param `q` from URL searchParams
       - Parse optional `script` param (default: 'iast')
       - Parse optional `mode` param: 'headword' (default), 'stem', 'search'
       - For mode 'headword': call lookupByHeadword
       - For mode 'stem': call lookupByStem
       - For mode 'search': call searchEntries
       - Return NextResponse.json with results
       - Return 400 if `q` param missing
       - Handle errors with 500 response

    7. Run full test suite: `npm test`
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/laksh/OneDrive/Documents/Codes/Unnamed Jish" && npm test</automated>
  </verify>
  <done>Dictionary lookup returns correct entries for MW and AP90 headwords. Stem index resolves inflected forms to stems. FTS5 search works across headwords and definitions. API route returns JSON for all three query modes. All dictionary and stem-index tests pass. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `npm test` passes all tests (transliteration, parser, dictionary, stem-index, app-shell)
2. `data/sanskrit.db` exists and contains MW + AP90 entries
3. `curl http://localhost:3000/api/dictionary?q=dharma` returns dictionary entries
4. `curl http://localhost:3000/api/dictionary?q=dharma&mode=search` returns FTS5 results
5. Stem index has entries from INRIA morphological data
</verification>

<success_criteria>
- CDSL parser correctly handles MW and AP90 entry formats
- SQLite database populated with 280K+ MW entries and AP90 entries
- Stem index populated from INRIA morphological data
- lookupByHeadword returns entries with full content (definition, grammar, etymology)
- lookupByStem resolves inflected forms through the reverse index
- searchEntries returns ranked FTS5 results
- API route serves dictionary data as JSON
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-text-input/01-02-SUMMARY.md`
</output>
